var documenterSearchIndex = {"docs":
[{"category":"section","location":"customization/#Customization","page":"Customization","text":"(Image: )\n\nEvery operating system has made unique design choices for application distribution. On macOS, applications are placed in the Applications folder via DMG containers. Windows supports many installer formats, with MSIX being the most modern. Linux uses Snap and Flatpak for distributing external software. Creating an installer on each platform involves a common set of tasks:\n\nProducing icon assets in the format the installer or operating system expects\nDeclaring the capabilities required by the application\nSpecifying the launch entry point, whether GUI or terminal\nBundling all configuration files with the application\nCode-signing the installer and, where required, the application itself\n\nMaintaining these platform-specific details is burdensome. AppBundler addresses this with sensible defaults that make shipping GUI applications straightforward. Where customization is needed, developers can apply a configuration overlay, keeping the process easy to debug and reason about.","title":"Customization"},{"category":"section","location":"customization/#How-It-Works","page":"Customization","text":"A build follows this pipeline:\n\nProject.toml          ← app name, version\nLocalPreferences.toml ← all build parameters\nmeta/                 ← optional file overrides\n        ↓\nappbundler build . --build-dir=build\n        ↓\nbuild/<name>.{msix,snap,dmg}\n\nMost builds require only a short LocalPreferences.toml. Build customization is done by placing files in meta/ that override AppBundler's built-in bundle templates — no changes to the core tool needed.\n\nIterating quickly: Use --debug for faster iteration when troubleshooting packaging or sandboxing issues — see Surgical Overrides.","title":"How It Works"},{"category":"section","location":"customization/#Command-Line-Parameters","page":"Customization","text":"Usage: appbundler build <project_dir> [OPTIONS]\n\nArguments:\n  <project_dir>                     Path to the Julia project to bundle\n\nOptions:\n  --build-dir DIR                   Output directory for the bundle\n                                    (default: temporary directory)\n                                    Use '@temp' to explicitly request a temp dir\n  --target-bundle {dmg|snap|appimage|msix}\n                                    Package format to produce\n                                    (default: platform native — dmg on macOS,\n                                    snap on Linux, msix on Windows)\n  --target-arch {x86_64|aarch64}    Target CPU architecture\n                                    (default: current system architecture)\n  --target-name NAME                Override the output file/directory name\n                                    (default: derived from app name and version)\n  --selfsign                        Sign the bundle with a self-signed certificate\n                                    (macOS / Windows; skips password prompt)\n  --password PASS                   Password for the signing certificate\n                                    (prompted interactively if omitted)\n  --force                           Overwrite an existing bundle at the target path\n  --debug                           Shorthand for --selfsign + uncompressed,\n                                    console-visible build; useful for quick iteration\n  -DKEY=VALUE                       Override a LocalPreferences.toml preference,\n                                    e.g. -Dbundler=\"juliac\"\n  -h, --help                        Show this help message\n\nExamples:\n  appbundler build .\n  appbundler build . --build-dir=build --force\n  appbundler build . --build-dir=@temp --debug\n  appbundler build . --target-bundle=snap --target-arch=aarch64\n  appbundler build . --selfsign --password=secret\n  appbundler build . -Dbundler=\"juliac\" -Djuliac.trim=true","title":"Command-Line Parameters"},{"category":"section","location":"customization/#Preferences","page":"Customization","text":"Parameters are read from LocalPreferences.toml and from Project.toml (for the module name and application version; LocalPreferences.toml can override these). The full list of available parameters is in joinpath(pkgdir(AppBundler), \"LocalPreferences.toml\").\n\nTo enable AppBundler preferences, add the following to your application's Project.toml; otherwise the preferences for AppBundler will not be registered:\n\n[extras]\nAppBundler = \"40eb83ae-c93a-480c-8f39-f018b568f472\"\n\nA typical LocalPreferences.toml is short:\n\n[AppBundler]\nwindowed = false\nbundler = \"juliac\"\njuliac.trim = true","title":"Preferences"},{"category":"section","location":"customization/#Quick-Reference","page":"Customization","text":"Parameter Default Description\nMetadata  \napp_name from Project.toml Application name\napp_exe from Project.toml Application exe name (lowercase)\nversion from Project.toml Application version\napp_summary — Short description for MSIX and Snap\napp_description — Longer description for Snap\npublisher_name — Publisher name\nbundle_identifier org.appbundler.{{app_exe}} Bundle identifier for DMG\nbuild_number git commit count Falls back to 0 if git is unavailable\nCommon  \nwindowed false Hide a console window at runtime\ncompress true Compress the application inside the bundle\nselfsign false Sign with a self-signed certificate\noverwrite_target false Overwrite target path (--force)\nBundler  \nbundler juliaimg Bundler to use: juliaimg or juliac\njuliaimg.mainless false Launch bin/julia directly without calling main\njuliaimg.precompile true Precompile project modules\njuliaimg.incremental false Build cache on top of Julia's own rather than starting fresh\njuliaimg.sysimg [] Packages to bake into the system image\njuliaimg.selective_assets false Enable selective asset inclusion (see AppEnv)\njuliac.trim false Enable trimming when compiling with juliac\nMSIX  \nmsix.path_length_threshold 260 Maximum allowed path length within the bundle\nmsix.skip_long_paths false Skip paths exceeding the threshold instead of erroring\nmsix.skip_symlinks true Skip symlinks\nmsix.skip_unicode_paths true Skip Unicode paths instead of erroring\nmsix.publisher \"CN=AppBundler, C=XX, O=PeaceFounder\" Publisher string for AppxManifest.xml\nmsix.bootstrapper false Create an exe installer from msix\nmsix.bootstrapper_windowed false Hide console window at installation\nDMG  \ndmg.shallow_signing true Sign only the top-level binary\ndmg.hardened_runtime true Enable hardened runtime during signing\ndmg.sandboxed_runtime false Restrict access to peripherals and system directories\ndmg.compression lzma Compression algorithm: bzip2, zlib, lzma, or lzfse\n\nBundler. The bundler choice determines which recipe files are applied and which juliaimg.* or juliac.* parameters are relevant. juliaimg.mainless is intended for Julia distributions that launch bin/julia directly rather than calling an application main. juliaimg.sysimg only needs top-level packages — dependencies are baked in automatically. juliaimg.selective_assets requires modules to be in the sysimage, since it removes all source files from the bundle; with the juliac bundler, selective assets are always used.\n\nMSIX. Windows does not support long paths inside bundles, so msix.path_length_threshold and msix.skip_long_paths exist to either warn or skip offending paths rather than erroring out. The msix.publisher string must match the signing certificate exactly; AppBundler reads it from the certificate at bundle time and inlines it into AppxManifest.xml automatically, so manual edits are rarely needed.\n\nDMG. dmg.shallow_signing must be set to false when submitting for Apple notarization, as all binaries must be signed individually. dmg.sandboxed_runtime restricts access to peripherals and system directories and should only be enabled if your application is designed to run in a sandbox.","title":"Quick Reference"},{"category":"section","location":"customization/#AppEnv","page":"Customization","text":"AppEnv is a small runtime support library that bridges the gap between a bundled Julia application and the host operating system. It handles three concerns that every bundled Julia app needs: environment setup, user data directories, and asset location.\n\nAppEnv is the first module loaded in juliaimg bundles. At runtime it configures LOAD_PATH and DEPOT_PATH so the application uses its compiled precompilation cache correctly. For Snap applications, the precompilation cache can be generated during installation via a configure hook, which AppEnv also provides.","title":"AppEnv"},{"category":"section","location":"customization/#User-Data-Directory","page":"Customization","text":"At launch, AppEnv sets the AppEnv.USER_DATA variable to a platform-appropriate writable location where the application can store settings, caches, and other persistent data. On Snap and MSIX the directory is managed by the operating system and is removed automatically when the application is uninstalled. The location can always be overridden by setting the USER_DATA environment variable before launch.\n\nDMG — ~/.config/<app_name> (the depot goes to ~/.cache/<app_name>)\nDMG (sandboxed) — ~/Library/Application Support/Local (detected via APP_SANDBOX_CONTAINER_ID)\nMSIX — %LOCALAPPDATA%\\Packages\\<bundle_identifier>_<hash>\\LocalState\nSnap — $SNAP_USER_DATA (set directly from the Snap environment variable)","title":"User Data Directory"},{"category":"section","location":"customization/#Asset-Management","page":"Customization","text":"AppEnv initializes pkgorigins from an index created at compile time. This allows assets to be placed within package directories and referenced via pkgdir(@__MODULE__) in a relocatable way, while only including a selected subset of files.\n\nAssets are declared per-module in LocalPreferences.toml using the assets key:\n\n[AppEnv]\nassets = [\"LICENSE\"]\n\n[QMLApp]\nassets = [\"src/App.qml\"]\n\n[AppBundler]\n# AppBundler options\n\nAssets are stored under assets/AppEnv and assets/QMLApp in the main directory. Package developers can declare their runtime assets here, while application developers can override them non-invasively.\n\nSelective assets are optional with juliaimg (enabled via juliaimg_selective_assets, which removes all source code from the bundle), but are the only mode available with the juliac bundler.","title":"Asset Management"},{"category":"section","location":"customization/#Application-Structure","page":"Customization","text":"When using JuliaC, the recommended entry point is:\n\nusing AppEnv\n\nfunction (@main)(ARGS)\n    AppEnv.init()\n    # Application logic; optionally reference AppEnv.USER_DATA\nend\n\nAppEnv.init() loads pkgorigins from a stored index within the compiled application so the runtime can locate assets and sets up USER_DATA. In juliaimg bundles it is called implicitly via etc/julia/startup.jl, so USER_DATA is available without any explicit call. With juliac, it must be called explicitly as shown above. In an interactive Julia session it does nothing, so it can be left in place during development without affecting application behaviour. It compiles correctly with JuliaC when trimming is enabled and is covered by the test suite.","title":"Application Structure"},{"category":"section","location":"customization/#Surgical-Overrides","page":"Customization","text":"AppBundler is designed for surgical customization through native file overrides. Files placed in the application's meta/ directory override AppBundler's default templates located at $(pkgdir(AppBundler))/recipes/. Templates are kept intentionally simple — rather than providing complex nested templates, AppBundler encourages copying and modifying complete configuration files, which keeps platform-specific customization straightforward to debug and communicate about.\n\nTip: Use --debug when working through override changes. It produces an uncompressed, self-signed bundle quickly and opens a console window so you can observe runtime behaviour without waiting for a full release build.","title":"Surgical Overrides"},{"category":"section","location":"customization/#Override-Locations","page":"Customization","text":"Format Directory Files\nDMG meta/dmg/ Entitlements.plist, Info.plist, DS_Store.toml, juliac_main.sh, juliaimg_main.sh\nMSIX meta/msix/ AppxManifest.xml, MSIXAppInstallerData.xml, resources.pri\nSnap meta/snap/ snap.yaml, main.desktop, juliaimg_main.sh, juliaimg_configure.sh\nAll meta/ icon.png, icon.icns, startup.jl (juliaimg only)","title":"Override Locations"},{"category":"section","location":"customization/#Icons","page":"Customization","text":"To use a custom application icon, place icon.png and icon.icns in the meta/ directory. During bundling, AppBundler checks for these files first and falls back to its built-in defaults if they are not present.","title":"Icons"},{"category":"section","location":"customization/#Template-Variables","page":"Customization","text":"Configuration files are Mustache templates. Variables are inlined at bundle time as capitalized versions of the preference names. For example, meta/snap/main.desktop:\n\n[Desktop Entry]\nName={{APP_DISPLAY_NAME}}\nExec={{APP_EXE}}\nIcon=${SNAP}/meta/icon.png\nVersion={{APP_VERSION}}\nComment={{APP_SUMMARY}}\nTerminal={{#WINDOWED}}false{{/WINDOWED}}{{^WINDOWED}}true{{/WINDOWED}}\nType=Application\nCategories=Utility;\n\nThe {{#WINDOWED}}...{{/WINDOWED}} / {{^WINDOWED}}...{{/WINDOWED}} pattern implements conditional logic driven by the windowed preference. When overriding a template, variables may be kept or replaced with static values.\n\nSome files are not installed directly into the bundle but are used as inputs during the build. meta/dmg/Entitlements.plist configures the sandbox baked into the signature of the main launcher, and meta/dmg/DS_Store.toml is a user-editable TOML representation of .DS_Store that is compiled into a binary .DS_Store before being placed in the DMG.","title":"Template Variables"},{"category":"section","location":"customization/#Bundler-Specific-Files","page":"Customization","text":"Some files only apply to a specific bundler, indicated by a juliac_ or juliaimg_ prefix. For instance, meta/snap/juliaimg_configure.sh is picked up only when using juliaimg. The equivalent for juliac is meta/snap/juliac_configure.sh which does not exist hence is ignored at stagging. meta/startup.jl is specific to juliaimg bundles — it is placed in etc/julia/ inside the bundle and is responsible for calling AppEnv.init() implicitly.","title":"Bundler-Specific Files"},{"category":"section","location":"customization/#Sandboxing-and-Capabilities","page":"Customization","text":"To customize sandboxing — such as granting access to hardware, networking, or custom launchers — override the relevant configuration file in your meta/ folder: Entitlements.plist for DMG, AppxManifest.xml for MSIX, and snap.yaml for Snap. Use --debug to iterate quickly when working through capability changes, and refer to the troubleshooting guide if the application does not behave as expected.","title":"Sandboxing and Capabilities"},{"category":"section","location":"troubleshooting/#Troubleshooting","page":"Troubleshooting","text":"Sandboxed execution is a requirement across macOS, Windows, and Linux for distribution through official app marketplaces, where confinement is typically a prerequisite for approval. AppBundler supports sandboxing for all three bundle formats — MSIX, Snap, and DMG. Sandbox support for Julia applications is not yet fully resolved: MSIX and Snap both have known open issues, while DMG sandboxing has a working solution.\n\nEach format implements isolation through its own mechanism, requiring format-specific configuration. Currently, macOS sandboxing is functional via the workaround described below; Snap confinement is limited to GTK applications; and MSIX sandboxing is non-functional for Julia applications due to a dependency on the legacy msvcrt32.dll runtime, which is incompatible with the UWP execution model.\n\nTo facilitate debugging, bundle compression can be disabled via the --debug flag. This also enables a console window through which stdout/stderr output is surfaced.","title":"Troubleshooting"},{"category":"section","location":"troubleshooting/#Snap-(Linux)","page":"Troubleshooting","text":"Snap confinement is currently broken for OpenGL applications; the root cause is unknown (see JuliaGraphics/QML.jl#191).\n\nTo debug Snap packages, skip compression and install the staging directory directly. Optionally, inspect the sandbox environment of the installed application:\n\nsnap try myapp_dir       # install unpackaged directory\nsnap run --shell myapp   # inspect sandbox environment interactively","title":"Snap (Linux)"},{"category":"section","location":"troubleshooting/#DMG-(macOS)","page":"Troubleshooting","text":"com.apple.coreservices.launchservicesd must be whitelisted as a sandbox exception; the underlying reason is not yet understood.\nNotarization is required for macOS distribution and can be performed with rcodesign.\nDeep signing (dmg_shallow_signing=false) may intermittently fail; if so, clean the staging directory and retry. For custom cleanup logic, use the public API as described in Reference.\nNotarization may fail due to an incompatible directory structure or unsigned artifacts; this has not yet been fully investigated.\n\nTo debug DMG bundles, skip the compression step. Note that the staging directory must carry a .app extension for macOS to treat it as an application bundle.","title":"DMG (macOS)"},{"category":"section","location":"troubleshooting/#MSIX-(Windows)","page":"Troubleshooting","text":"GTK applications fail to resolve shared libraries within the UWP execution environment. This is expected to be resolved once Julia migrates to the ucrt.dll runtime.\nOnly the packagedClassicApp execution mode is supported due to Julia's msvcrt32.dll dependency. This constraint will be lifted once Julia transitions to ucrt.dll.\n\nTo debug MSIX packages, skip compression and register the manifest from the staging directory directly:\n\nAdd-AppPackage -register AppxManifest.xml","title":"MSIX (Windows)"},{"category":"section","location":"formats/msix/#MSIX","page":"MSIX","text":"MSIX is the modern Windows application package format. AppBundler builds .msix packages with a fully open-source toolchain, so packages can be created on any platform without the Windows SDK.\n\n(Image: MSIX installation)","title":"MSIX"},{"category":"section","location":"formats/msix/#Build-pipeline","page":"MSIX","text":"The standard Microsoft workflow packs a staging directory with MakeAppx and signs it with SignTool. Because these Windows SDK tools cannot be redistributed, AppBundler uses open-source equivalents:\n\nStagingDir → msixpack → osslsigncode → MyApp.msix\n\nBoth tools are cross-compiled with BinaryBuilder, which makes it possible to build and sign MSIX packages from Linux and macOS as well as Windows.","title":"Build pipeline"},{"category":"section","location":"formats/msix/#Package-contents","page":"MSIX","text":"Before packing, AppBundler assembles StagingDir/ with the following layout:\n\nPath Purpose\nAppxManifest.xml Package identity, entry point, and capabilities\nresources.pri Resource index; required for icons to render correctly\nAssets/ Application icons\nMsix.AppInstaller.Data/MSIXAppInstallerData.xml App Installer configuration\nMyApp.exe Application launcher\n\nSandboxing and permissions are set through the capabilities in AppxManifest.xml. The target system must have the Universal C Runtime (UCRT).\n\nwarning: GUI subsystem and console processes\nAppBundler switches MyApp.exe to the Windows GUI subsystem so that no console window appears on launch. For GUI applications this currently leaves a zombie console process behind. This is a known limitation.","title":"Package contents"},{"category":"section","location":"formats/msix/#Code-signing","page":"MSIX","text":"For a user-friendly installation, the MSIX must be signed with a certificate chaining to a trusted root. Options as of September 2026 include:\n\nMicrosoft Store signing (free, subject to review)\nAzure Artifact Signing (€10/month)\nCertum Open Source Code Signing (€25/year)\n\nWithin an organisation, administrators may also deploy an internal trusted root to company machines and sign applications with it.\n\nAppBundler currently expects a password-protected .pfx file, while many providers deliver keys on hardware tokens or through cloud signing services. Hardware token support is planned. Until then, build with --skipsign and sign separately, or use the lower-level signing API described in reference.md.\n\nThe publisher in AppxManifest.xml must exactly match the certificate subject, including order, spaces, and commas. Set it in LocalPreferences.toml:\n\nmsix_publisher = \"CN=AppBundler, C=XX, O=PeaceFounder\"","title":"Code signing"},{"category":"section","location":"formats/msix/#Self-signing","page":"MSIX","text":"For testing, AppBundler can generate a self-signed certificate at msix/certificate.pfx:\n\nAppBundler.install_msix_certificate(\"msix/certificate.pfx\")\n\nThe generated password is printed to stdout. Pass it to the build with --password=mypassword. For a quick local build, use --selfsign instead: it signs with a throwaway certificate and ignores msix/certificate.pfx.\n\nEither way, the package is trusted only on machines where its certificate has been installed, as described below.","title":"Self-signing"},{"category":"section","location":"formats/msix/#Installing-self-signed-packages","page":"MSIX","text":"A self-signed package installs only after its certificate is added to the trusted root authorities, which can be done manually from the package properties (guide). To automate this, AppBundler provides recipes/msix/bootstrap.ps1, which extracts the certificate, trusts it, and launches the installer:\n\nbootstrap.ps1 myapp.msix           # graphical installer\nbootstrap.ps1 myapp.msix -Console  # console installation\n\nThis also enables one-line web installs: a hosted install.ps1 can download the MSIX and bootstrap.ps1 and run bootstrap.ps1 myapp.msix -Console, invoked with e.g.:\n\nirm https://example.com/install.ps1 | iex","title":"Installing self-signed packages"},{"category":"section","location":"formats/msix/#Installer-EXE","page":"MSIX","text":"AppBundler can wrap the MSIX in a 7-Zip self-extracting executable. On launch it extracts the package and bootstrap.ps1 to a temporary directory and runs bootstrap.ps1 myapp.msix.\n\nEnable it with -Dmsix2exe=true or persistently with msix2exe = true in LocalPreferences.toml. Set msix2exe_windowed = true to launch the graphical installer after extraction.","title":"Installer EXE"},{"category":"section","location":"formats/msix/#API","page":"MSIX","text":"msix_config = MSIX(project; selfsign = true)\n\nbundle(msix_config, msix_archive) do app_stage\n    # install files into app_stage\nend\n\nmsix2exe_config = MSIX2EXE(project)\nrepack(msix_archive, msix2exe_config, exe_archive)","title":"API"},{"category":"type","location":"formats/msix/#AppBundler.MSIX","page":"MSIX","text":"MSIX([project]; arch, compress, windowed, kwargs...)\n\nCreate an MSIX configuration object for Windows application packaging.\n\nWhen project is provided, configuration files are searched in project, then project/meta, then the built-in recipes directory. Application parameters (APP_NAME, APP_VERSION, etc.) are read from project/Project.toml, and packaging defaults (path_length_threshold, selfsign, etc.) are read from project/LocalPreferences.toml. Without project, only the built-in recipes and the active project's LocalPreferences.toml are used.\n\nArguments\n\nproject: Path to a project directory containing Project.toml, optional LocalPreferences.toml, and optional meta/msix/ overrides\n\nKeyword Arguments\n\nprefix = joinpath(dirname(@__DIR__), \"recipes\"): Base directory or array of directories to search for configuration files in sequential order\npreferences: Dictionary of packaging preferences used for the defaults below; read from project when given, otherwise from the active project\nicon = get_path(prefix, [\"msix/Assets\", \"msix/icon.png\", \"icon.png\"]; dir = true): Path to application icon file or Assets directory\nappxmanifest = get_path(prefix, \"msix/AppxManifest.xml\"): Path to MSIX application manifest template\ncommand::Cmd: Command launching the application; defaults to msix.command preference. Its executable and escaped arguments are exposed to the manifest template as COMMAND_EXE and COMMAND_ARGS\nresources_pri = get_path(prefix, \"msix/resources.pri\"): Path to package resource index file\nmsixinstallerdata = get_path(prefix, \"msix/MSIXAppInstallerData.xml\"): Path to installer configuration template\npath_length_threshold: Maximum allowed path length; defaults to msix.path_length_threshold preference\nskip_long_paths: If true, skip files exceeding path length threshold; if false, throw an error; defaults to msix.skip_long_paths preference\nskip_symlinks: If true, skip file and directory symlinks; defaults to msix.skip_symlinks preference\nskip_unicode_paths: If true, skip files with non-ASCII paths; defaults to msix.skip_unicode_paths preference\nselfsign: If true, generate a temporary self-signed certificate instead of using pfx_cert; defaults to selfsign preference\npublisher: Publisher string embedded in the manifest (e.g. \"CN=Example, O=Example Ltd\"); defaults to msix.publisher preference, normalized to \", \"-separated fields\npfx_cert = get_path(prefix, \"msix/certificate.pfx\"): Path to code signing certificate; nothing when the skipsign preference is set\nwindowed: If true, the application runs without a console window; defaults to windowed preference\ncompress: If true, pack the staging directory into an .msix archive; defaults to compress preference\narch = Sys.ARCH: Target CPU architecture\npredicate: Bundler predicate used for hook selection; defaults to bundler preference\nparameters: Dictionary of parameters for Mustache template rendering. Always contains WINDOWED, PUBLISHER, COMMAND_EXE and COMMAND_ARGS, derived from the keywords above. When project is provided, it is further populated from Project.toml and preferences: APP_NAME, APP_DISPLAY_NAME, APP_VERSION, BUILD_NUMBER, APP_SUMMARY, APP_DESCRIPTION, BUNDLE_IDENTIFIER, PUBLISHER_DISPLAY_NAME, and MODULE_NAME (Julia-based bundles only)\n\nExamples\n\nMSIX()                                    # default recipes only\nMSIX(app_dir)                             # project with Project.toml parameters\nMSIX(app_dir; skip_long_paths = true)     # project with keyword overrides\nMSIX(app_dir; command = `bin\\myapp.exe --flag \"a b\"`)  # custom launch command\nMSIX(; prefix = [\"custom/\", \"recipes/\"])  # explicit search path\n\n\n\n\n\n","title":"AppBundler.MSIX"},{"category":"type","location":"formats/msix/#AppBundler.MSIX2EXE","page":"MSIX","text":"MSIX2EXE([project]; prefix, preferences, bootstrap, windowed, sfx_stub, title)\n\nCreate an MSIX-to-EXE configuration object for creating self-extracting Windows installers from MSIX packages.\n\nThe resulting installer is a 7-Zip-based self-extracting executable. When launched, the executable extracts the MSIX package and the configured PowerShell bootstrap script to a temporary directory and then executes the bootstrap script with the extracted MSIX package as its argument.\n\nThis is useful for distributing self-signed MSIX packages, since the bootstrap script can extract and install the package's signing certificate in the system's root, before launching installer on the MSIX installer.\n\nWhen project is provided, configuration files are searched in project, then project/meta, then the built-in recipes directory. Without project, only the built-in recipes and the active project's LocalPreferences.toml are used.\n\nArguments\n\nproject: Path to a project directory containing optional Project.toml, LocalPreferences.toml, and optional meta/msix/ overrides\n\nKeyword Arguments\n\nprefix = joinpath(dirname(@__DIR__), \"recipes\"): Base directory or array of directories to search for configuration files in sequential order\n`bootstrap: Path to the bootstrap script which is embedded in the self-extracting installer. The script is invoked with the extracted MSIX package as its first argument\nwindowed = preferences[\"msix2exe_windowed\"]: If true, run the bootstrap process without displaying a console window and launch graphical MSIX installer\nsfx_stub = get(preferences, \"msix2exe_sfx_stub\", MSIX2EXEPack.extract_stub()): Path to the 7-Zip self-extracting executable stub used to construct the installer\ntitle = \"Installer\": Title displayed by the self-extracting installer\n\nBootstrap Script\n\nThe bootstrap script is invoked after the embedded files have been extracted, with the MSIX package path supplied as its first argument. For example:\n\npowershell.exe bootstrap.ps1 msix_archive.msix\n\n\n\n\n\n","title":"AppBundler.MSIX2EXE"},{"category":"section","location":"reference/#Reference","page":"Reference","text":"AppBundler is structured around two concepts: a product spec, which describes how to build a Julia application, and a bundle format, which describes how to package it for distribution on a target platform. These are combined through the bundle function:\n\nbundle(spec, format, destination; password = \"\")\n\nThis is also the function exposed to the command-line API, where command-line parameters configure the spec and format fields.","title":"Reference"},{"category":"section","location":"reference/#Product-Specs","page":"Reference","text":"A product spec defines how the application is compiled or staged. There are two kinds:\n\nspec = JuliaImgBundle(project; kwargs...)  # stages a self-contained Julia image\nspec = JuliaCBundle(project; kwargs...)    # compiles a native executable via juliac\n\nBoth specs can be staged directly into a directory for inspection before packaging via stage(spec, destination). For JuliaImgBundle, staging is platform-agnostic as long as compilation is not required — cross-platform staging is possible by setting precompile=false and sysimg_packages = [], with the target platform specified via the platform keyword (defaults to HostPlatform()). For JuliaCBundle, the platform is fixed to the host, as determined by the juliac executable on PATH.","title":"Product Specs"},{"category":"section","location":"reference/#Bundle-Formats","page":"Reference","text":"A bundle format defines the packaging target. The supported formats are DMG (macOS), MSIX (Windows), and Snap and AppImage (Linux). They are instantiated from a project directory:\n\ndmg      = DMG(project; arch = Sys.ARCH, kwargs...)\nmsix     = MSIX(project; arch = Sys.ARCH, kwargs...)\nsnap     = Snap(project; arch = Sys.ARCH, kwargs...)\nappimage = AppImage(project; arch = Sys.ARCH, kwargs...)\n\nAppImage produces a single mountable file rather than an installed tree; see AppImage.\n\nEach format reads configuration file overrides from the corresponding project/meta/<format> directory and carries architecture information that determines the destination platform. Bundle formats can also be staged independently via stage(format, destination) to produce the directory structure before compression and signing.","title":"Bundle Formats"},{"category":"section","location":"reference/#Low-Level-Bundle-API","page":"Reference","text":"For packaging non-Julia applications, or when you need full control over what goes into the bundle, the lower-level bundle do-block API can be used directly:\n\nbundle(format, destination; password = \"\") do staging_dir\n    # copy or compile application files into staging_dir\nend\n\nThe password argument is the certificate password used to decrypt the signing certificate and perform code signing during the pack step. Non-Julia applications can be bundled this way as well, as long as the user takes care of building and installing the application files in the setup callback.","title":"Low-Level Bundle API"},{"category":"section","location":"reference/#Types","page":"Reference","text":"AppBundler.DMG AppBundler.Snap AppBundler.AppImage","title":"Types"},{"category":"section","location":"reference/#Functions","page":"Reference","text":"","title":"Functions"},{"category":"type","location":"reference/#AppBundler.JuliaImg.JuliaImgBundle","page":"Reference","text":"JuliaImgBundle(project; kwargs...)\n\nBuild specification for staging a Julia application as a self-contained image.\n\nThe Julia version is read from Manifest.toml in project (falling back to the running Julia version). Incremental compilation defaults to true when no sysimage packages are requested; building a sysimage forces incremental = false because a new sysimage invalidates all existing pkgimage caches.\n\nArguments\n\nproject::String: Path to the application directory containing Project.toml and Manifest.toml\n\nKeyword Arguments\n\nprecompile::Bool = true: Precompile packages during staging. When false, precompilation is deferred to the first launch on the target system\nincremental::Bool = isempty(sysimg_packages): Whether to reuse existing pkgimage caches. Automatically set to false when sysimg_packages is non-empty\nsysimg_packages::Vector{String} = []: Packages to bake into the system image. Triggers a full sysimage rebuild, which invalidates all pkgimage caches\nsysimg_args::Cmd =``: Extra command-line arguments forwarded to the sysimage compiler\nremove_sources::Bool = false: Strip .jl source files from the staged package tree (only meaningful when all relevant packages are baked into the sysimage)\nasset_spec::Dict{Symbol,Vector{String}} = Dict(): Selective asset inclusion rules. When empty, all artifacts are included and indexed via pkgorigin\nasset_rpath::String = \"assets\": Destination subdirectory for assets when asset_spec is set\nstartup_file::String: Path to the startup.jl template. Resolved from meta/startup.jl inside project, falling back to $(pkgdir(AppBundler))/recipes/startup.jl\n\nExamples\n\n# Default: precompile everything, incremental caches\npkg = JuliaImgBundle(\"path/to/app\")\n \n# Skip precompilation — compile on first launch instead\npkg = JuliaImgBundle(\"path/to/app\"; precompile = false)\n \n# Bake heavy dependencies into a sysimage (forces non-incremental rebuild)\npkg = JuliaImgBundle(\"path/to/app\"; sysimg_packages = [\"Plots\", \"DifferentialEquations\"])\n\n\n\n\n\n","title":"AppBundler.JuliaImg.JuliaImgBundle"},{"category":"type","location":"reference/#AppBundler.JuliaC.JuliaCBundle","page":"Reference","text":"JuliaCBundle(project; kwargs...)\n\nBuild specification for compiling a Julia application into a native executable via juliac.\n\nUnlike JuliaImgBundle, which stages a full Julia runtime alongside precompiled package images, JuliaCBundle ahead-of-time compiles the application into a standalone native executable. The juliac tool must be installed and is looked up in bin/juliac under each entry of DEPOT_PATH, with ~/.julia/bin as a final fallback.\n\nArguments\n\nproject::String: Path to the application directory containing Project.toml\n\nKeyword Arguments\n\njuliac_cmd::Cmd = Cmd([juliac()]): Command used to invoke juliac. Defaults to the first juliac executable found on DEPOT_PATH\ntrim::Bool = false: When true, passes --trim=safe to juliac, removing unreachable code from the output binary\nargs::Cmd =`: Additional arguments forwarded verbatim tojuliac`\nasset_spec::Dict{Symbol,Vector{String}} = Dict(): Selective asset inclusion rules. When empty, no assets are copied into the bundle\nasset_rpath::String = \"assets\": Destination subdirectory for assets inside destination\n\nExamples\n\n# Minimal: compile with defaults\npkg = JuliaCBundle(\"path/to/app\")\n \n# Enable dead-code trimming and a custom executable name\npkg = JuliaCBundle(\"path/to/app\"; executable_name = \"myapp\", trim = true)\n\n\n\n\n\n","title":"AppBundler.JuliaC.JuliaCBundle"},{"category":"method","location":"reference/#AppBundler.stage-Tuple{AppBundler.JuliaImg.JuliaImgBundle, String}","page":"Reference","text":"stage(product::JuliaImgBundle, destination::String;\n      platform::AbstractPlatform = HostPlatform(),\n      cpu_target = get_cpu_target(platform),\n      runtime_mode = \"MIN\",\n      app_name = \"\",\n      bundle_identifier = \"\")\n\nStage a Julia application into destination, ready for packaging and distribution.\n\nThe staging process:\n\nDownloads and extracts the Julia runtime for platform (version from Manifest.toml)\nCopies all non-stdlib packages and artifacts into the stdlib tree\nConfigures startup.jl, DEPOT_PATH, and LOAD_PATH via AppEnv\nOptionally compiles a system image from product.sysimg_packages\nOptionally precompiles pkgimages for all project dependencies\n\nWhen product.precompile is true, platform must match the host OS and architecture (see cross-compilation constraints below).\n\nArguments\n\nproduct::JuliaImgBundle: Staging configuration\ndestination::String: Directory in which the staged application is assembled\n\nKeyword Arguments\n\nplatform::AbstractPlatform: Target platform; defaults to the current host\ncpu_target: LLVM CPU target string; derived from platform by default\nruntime_mode: AppEnv runtime mode string passed to AppEnv.save_config\napp_name: Application name embedded in the AppEnv config\nbundle_identifier: Bundle identifier embedded in the AppEnv config (e.g. reverse-DNS on macOS)\n\nCross-Compilation Constraints\n\nPrecompilation runs native code, so cross-OS and cross-architecture compilation is not supported when product.precompile = true:\n\nWindows: host must be Windows\nmacOS: host must be macOS; x86_64 → aarch64 is not supported\nLinux: host must be Linux with a matching architecture\n\nSet precompile = false and sysimg_packages = [] in JuliaImgBundle to stage for a different platform without compilation.\n\nExamples\n\npkg = JuliaImgBundle(\"src/MyApp\")\n \n# Stage for the current machine\nstage(pkg, \"build/staging\")\n \n# Stage for macOS arm64 (must run on Apple Silicon with precompile=false, or natively)\nstage(pkg, \"build/MyApp.app/Contents/Resources/julia\";\n      platform = Platform(\"aarch64\", \"macos\"))\n \n# Embed app identity in the AppEnv config\nstage(pkg, \"build/staging\"; app_name = \"MyApp\", bundle_identifier = \"com.example.myapp\")\n\n\n\n\n\n","title":"AppBundler.stage"},{"category":"method","location":"reference/#AppBundler.stage-Tuple{AppBundler.JuliaC.JuliaCBundle, String}","page":"Reference","text":"stage(spec::JuliaCBundle, destination::String;\n      runtime_mode = \"MIN\",\n      app_name = get_module_name(spec.project),\n      bundle_identifier = \"\")\n\nCompile a Julia application into a native executable and assemble it in destination.\n\nThe staging process:\n\nSaves an AppEnv config to destination/config with runtime identity and load-path settings\nInstalls assets from spec.asset_spec into destination/<asset_rpath>\nWrites a pkgorigin index to destination/index for asset resolution at runtime\nInvokes juliac to AOT-compile the application and bundle the result into destination\n\nUnlike JuliaImgBundle, no Julia runtime tarball is downloaded — juliac produces a self-contained native binary. The host toolchain must be compatible with the target.\n\nArguments\n\nspec::JuliaCBundle: Compilation and asset configuration\ndestination::String: Directory in which the compiled application is assembled\n\nKeyword Arguments\n\nruntime_mode: AppEnv runtime mode string passed to AppEnv.save_config\napp_name: Application name embedded in the AppEnv config; defaults to the module name\nbundle_identifier: Bundle identifier embedded in the AppEnv config (e.g. reverse-DNS on macOS)\n\nExamples\n\npkg = JuliaCBundle(\"src/MyApp\")\n \n# Stage into a directory\nstage(pkg, \"build/myapp\";\n      app_name = \"MyApp\", bundle_identifier = \"com.example.myapp\")\n \n# Stage with a custom runtime mode\nstage(pkg, \"build/staging\"; runtime_mode = \"SANDBOX\")\n\n\n\n\n\n","title":"AppBundler.stage"},{"category":"method","location":"reference/#AppBundler.stage-Tuple{AppBundler.MSIX, String}","page":"Reference","text":"stage(config, destination::String; [dsstore=false])\n\nStage package metadata and directory structure into destination in preparation for bundling.\n\nconfig is a format-specific configuration object — MSIX, DMG, or Snap — that carries the template files, parameters, and settings for the target platform. Mustache-rendered templates are written using the parameters stored in config.\n\nstage is called automatically by bundle, but can be used directly when you need to inspect or modify the staging directory before compression and signing.\n\nStaged layout by format\n\nMSIX\n\nAssets/ — application icons (generated from source or copied verbatim if already a directory)\nAppxManifest.xml — rendered package manifest\nresources.pri — package resource index\nMsix.AppInstaller.Data/MSIXAppInstallerData.xml — rendered installer configuration\n\nDMG\n\nContents/Resources/icon.icns — application icon\nContents/Info.plist — rendered application metadata\nContents/MacOS/<app-name> (optional) — native launcher when main_launcher is set\n\nWhen dsstore = true, also writes into the parent of destination:\n\nApplications — symlink to /Applications for drag-and-drop installation\n.DS_Store — custom Finder window appearance\n\nSnap\n\nmeta/icon.png — application icon\nmeta/snap.yaml — rendered Snap package metadata\nmeta/gui/<app-name>.desktop — rendered desktop launcher\nmeta/hooks/configure (optional) — configuration hook when configure_hook is set\nbin/<app-name> (optional) — main launcher script when main_launcher is set\n\nExamples\n\nstage(MSIX(app_dir), \"build/msix_staging\")\nstage(DMG(app_dir),  \"build/MyApp.app\"; dsstore = true)\nstage(Snap(app_dir), \"build/snap_staging\")\n\n\n\n\n\n","title":"AppBundler.stage"},{"category":"method","location":"reference/#AppBundler.bundle-Tuple{Function, AppBundler.DMG, String}","page":"Reference","text":"bundle(setup::Function, config, destination::String; force=false, [password=\"\"])\n\nStage, populate, and optionally compress an application bundle for distribution.\n\nconfig is a format-specific configuration object — MSIX (Windows), DMG (macOS), or Snap (Linux) — and destination is the path of the final artifact (e.g. \"MyApp.msix\", \"MyApp.dmg\", \"MyApp.snap\") or an uncompressed staging directory.\n\nThe function follows three steps:\n\nStage — writes platform metadata and directory structure into a staging area via stage.\nSetup — calls setup(staging_dir), where you copy or compile the application files that should be included in the bundle.\nPack — when config.compress is true (the default when destination carries the format extension), compresses the staging area into the final artifact and performs code signing.\n\nSet force = true to overwrite an existing destination path.\n\nCode signing\n\nMSIX and DMG sign the bundle automatically during the pack step. Pass the certificate password via the password keyword argument (defaults to \"\"). When config.selfsign is true, a temporary self-signed certificate is generated instead of using the one in the configuration. DMG entitlements are rendered from the template stored in the configuration. Snap packages are not signed locally; they are verified by the Snap Store after upload.\n\nExamples\n\nbundle(MSIX(app_dir), \"MyApp.msix\") do staging_dir\n    # copy or compile application files into staging_dir\nend\n \nbundle(DMG(app_dir), \"MyApp.dmg\") do staging_dir\n    # copy or compile application files into staging_dir\nend\n \nbundle(Snap(app_dir), \"MyApp.snap\") do staging_dir\n    # copy or compile application files into staging_dir\nend\n\n\n\n\n\n","title":"AppBundler.bundle"},{"category":"method","location":"reference/#AppBundler.bundle-Tuple{AppBundler.JuliaC.JuliaCBundle, AppBundler.AppImage, String}","page":"Reference","text":"bundle(product::JuliaCBundle, appimage::AppImage, destination::String; force = false)\n\nPackage a juliac-compiled executable as a mountable AppImage.\n\n\n\n\n\n","title":"AppBundler.bundle"},{"category":"method","location":"reference/#AppBundler.bundle-Tuple{AppBundler.JuliaImg.JuliaImgBundle, AppBundler.DMG, String}","page":"Reference","text":"bundle(product, packaging, destination; force=false, [password=\"\"])\n\nBundle a Julia application product into a platform-specific package at destination by calling bundle(setup, config, destination) with product staged as the setup step.\n\nArguments\n\nproduct: The application artifact to bundle. Either a JuliaImgBundle or a JuliaCBundle \npackaging: The target package format. One of:\nDMG — macOS disk image (.dmg). Must be built on macOS.\nSnap — Linux Snap package. Must be built on Linux.\nMSIX — Windows app package (.msix). Must be built on Windows.\ndestination: Output path for the produced package file.\n\nKeyword Arguments\n\nforce=false: Overwrite an existing package at destination if true.\npassword=\"\": Code-signing certificate password. Applicable to DMG and MSIX targets only.\n\nA warning is emitted when the host OS does not match the target platform, as cross-platform packaging is not supported.\n\nSee also stage, bundle(setup, config, destination).\n\n\n\n\n\n","title":"AppBundler.bundle"},{"category":"section","location":"formats/dmg/#DMG","page":"DMG","text":"DMG is the standard disk image format for distributing macOS applications. AppBundler builds .dmg images with a fully open-source toolchain, so they can be created and signed on any platform without Xcode.\n\n(Image: DMG installation)","title":"DMG"},{"category":"section","location":"formats/dmg/#Build-pipeline","page":"DMG","text":"The standard Apple workflow signs the app bundle with codesign, packs it into a disk image with hdiutil, signs the image, submits it for notarization with notarytool, and attaches the notarization ticket with stapler:\n\nMyApp.app → codesign → hdiutil → codesign → notarytool → stapler\n\nThese tools are available only on macOS. AppBundler uses open-source equivalents instead:\n\nMyApp.app → rcodesign → xorriso → dmg → rcodesign notarize\n\nHere dmg comes from libdmg-hfsplus, and rcodesign handles both code signing and notarization. All tools are redistributable and cross-compiled with BinaryBuilder, so DMG images can be built and signed from Linux and Windows as well as macOS.","title":"Build pipeline"},{"category":"section","location":"formats/dmg/#Package-contents","page":"DMG","text":"Before creating the image, AppBundler assembles the MyApp.app bundle with the following layout:\n\nPath Purpose\nContents/MacOS/MyApp Application launcher\nContents/Libraries/main Binary stub reference\nContents/Info.plist Application metadata\nContents/Resources/icon.icns Application icon\n../DS_Store Controls the installer's appearance\n\nSandboxing and permissions are configured through Entitlements.plist, which is embedded in the launcher's signature during code signing.\n\nwarning: Native launcher required\nThe launcher must be a native binary, since macOS code signing and entitlements cannot be applied to scripts.","title":"Package contents"},{"category":"section","location":"formats/dmg/#Code-signing","page":"DMG","text":"Apple issues code signing certificates only to members of the Apple Developer Program, which costs €99/year as of September 2026. Distribution outside the Mac App Store requires a Developer ID Application certificate, and only the account holder can create one. A standalone certificate.pfx can be produced on any platform with OpenSSL:\n\nGenerate a private key and a certificate signing request:\n\n   openssl genrsa -out developer_id.key 2048\n   openssl req -new -key developer_id.key -out developer_id.csr \\\n       -subj \"/emailAddress=you@example.com/CN=Your Name/C=LV\"\n\nIn the Apple Developer portal, create a new Developer ID Application certificate, upload developer_id.csr, and download the resulting .cer file.\nCombine the certificate and key into a password-protected .pfx:\n\n   openssl x509 -inform DER -in developerID_application.cer -out developer_id.pem\n   openssl pkcs12 -export -inkey developer_id.key -in developer_id.pem -out dmg/certificate.pfx\n\nNotarization additionally requires an App Store Connect API key, created in App Store Connect under Users and Access → Integrations.","title":"Code signing"},{"category":"section","location":"formats/dmg/#Notarization","page":"DMG","text":"macOS codesigning has an additional requirement beyond certificate signing: Apple requires all distributed applications to be notarized. Notarization means submitting the bundle to Apple's servers, where it is checked for proper structure and the absence of malware. Two settings must be enabled in LocalPreferences.toml for a bundle to pass notarization:\n\ndmg.shallow_signing = false\ndmg.hardened_runtime = true\n\nNote on shallow vs. deep signing: Deep signing is disabled by default because it takes considerable time for Julia applications and currently tends to fail with rcodesign deep signing. Unfortunately, codesign --verify --deep --verbose=4 myapp.app passes even with shallow signing, so the only reliable way to verify that deep signing is correct is to submit the bundle to Apple's notary service and inspect the response. Budget some time for this when setting up notarization for the first time.","title":"Notarization"},{"category":"section","location":"formats/dmg/#Self-signing","page":"DMG","text":"For testing, AppBundler can generate a self-signed certificate at dmg/certificate.pfx:\n\nAppBundler.install_dmg_certificate(\"dmg/certificate.pfx\")\n\nThe generated password is printed to stdout. Pass it to the build with --password=mypassword. For a quick local build, use --selfsign instead: it signs with a throwaway certificate and ignores dmg/certificate.pfx.\n\nApple notarizes only packages signed with a Developer ID certificate, so self-signed builds cannot be notarized. Gatekeeper blocks them by default, and they must be installed as described below.","title":"Self-signing"},{"category":"section","location":"formats/dmg/#Installing-self-signed-packages","page":"DMG","text":"Gatekeeper blocks applications that are not signed with a Developer ID certificate and notarized. Users must explicitly allow them, as described in Apple's guide to opening apps from an unknown developer. To automate this, AppBundler provides recipes/dmg/bootstrap.sh. It mounts the DMG, copies the app to /Applications, and removes the quarantine attribute:\n\nbootstrap.sh myapp.dmg\n\nThis also enables one-line web installs: a hosted install.sh can download the DMG and bootstrap.sh and run bootstrap.sh myapp.dmg, invoked with e.g.:\n\ncurl -fsSL https://example.com/install.sh | sh","title":"Installing self-signed packages"},{"category":"section","location":"formats/dmg/#API","page":"DMG","text":"dmg_config = DMG(project; selfsign = true)\n\nbundle(dmg_config, dmg_archive) do app_stage\n    # install files into app_stage\nend","title":"API"},{"category":"type","location":"formats/dmg/#AppBundler.DMG","page":"DMG","text":"DMG([project]; arch, compress, windowed, kwargs...)\n\nCreate a DMG configuration object for macOS application packaging.\n\nWhen project is provided, configuration files are searched in project, then project/meta, then the built-in recipes directory. Application parameters (APP_NAME, APP_VERSION, etc.) are read from project/Project.toml, and packaging defaults (selfsign, compression, etc.) are read from project/LocalPreferences.toml. Without project, only the built-in recipes and the active project's LocalPreferences.toml are used.\n\nArguments\n\nproject: Path to a project directory containing Project.toml, optional LocalPreferences.toml, and optional meta/dmg/ overrides\n\nKeyword Arguments\n\nprefix = joinpath(dirname(@__DIR__), \"recipes\"): Base directory or array of directories to search for configuration files in sequential order\nicon = get_path(prefix, [\"dmg/icon.icns\", \"dmg/icon.png\", \"icon.icns\"]): Path to application icon (.icns or .png)\ninfo_config = get_path(prefix, \"dmg/Info.plist\"): Path to Info.plist template with app metadata\ncommand::Cmd: Command launching the application; defaults to dmg.command preference. Its executable and escaped arguments are exposed to the templates as COMMAND\nentitlements = get_path(prefix, \"dmg/Entitlements.plist\"): Path to entitlements file for code signing\ndsstore = get_path(prefix, [\"dmg/DS_Store.toml\", \"dmg/DS_Store\"]): Path to DS_Store file or TOML template for Finder window appearance\nselfsign: If true, generate a temporary self-signed certificate instead of using pfx_cert; defaults to selfsign preference\npfx_cert = get_path(prefix, \"dmg/certificate.pfx\"): Path to code signing certificate\nshallow_signing: If true, sign only the top-level bundle rather than all nested binaries; defaults to dmg.shallow_signing preference\nhardened_runtime: If true, enable hardened runtime during signing (required for notarization); defaults to dmg.hardened_runtime preference\nsandboxed_runtime: If true, enable the App Sandbox entitlement; defaults to dmg.sandboxed_runtime preference\nmain_launcher: Path to the Julia entry-point script. When set, a native redirect launcher is installed at Contents/MacOS/<app_name> and the script itself at Contents/Libraries/main; resolved from prefix using the bundler predicate; omitted if not found\nhfsplus = false: If true, use HFS+ filesystem when building the disk image otherwise uses ISO\nwindowed: If true, the application runs without a console window; defaults to windowed preference\ncompress: If true, pack the staging directory into a .dmg disk image; defaults to compress preference\ncompression: Compression algorithm for the disk image (:lzma, :bzip2, :zlib, or :lzfse); defaults to dmg.compression preference\narch = Sys.ARCH: Target CPU architecture\npredicate: Bundler predicate used for hook selection; defaults to bundler preference\nparameters: Dictionary of parameters for Mustache template rendering. When project is provided, pre-populated from Project.toml and preferences: APP_NAME, APP_DISPLAY_NAME, APP_VERSION, BUILD_NUMBER, APP_SUMMARY, APP_DESCRIPTION, BUNDLE_IDENTIFIER, PUBLISHER_DISPLAY_NAME, MODULE_NAME (Julia-based bundles only), WINDOWED, and SANDBOXED_RUNTIME\n\nExamples\n\nDMG()                                    # default recipes only\nDMG(app_dir)                             # project with Project.toml parameters\nDMG(app_dir; hardened_runtime = false)   # project with keyword overrides\nDMG(; prefix = [\"custom/\", \"recipes/\"]) # explicit search path\n\n\n\n\n\n","title":"AppBundler.DMG"},{"category":"section","location":"formats/appimage/#AppImage","page":"AppImage","text":"warning: Experimental\nAppImage bundling is experimental. Its API, defaults, and on-disk layout may change in any release without deprecation, and existing build scripts or bundles may stop working.\n\nAppImage is a single executable file for Linux that is mounted rather than installed. It suits applications made of thousands of small files, which are slow to unpack and hydrate on the network filesystems used by HPC clusters.","title":"AppImage"},{"category":"section","location":"formats/appimage/#Build-pipeline","page":"AppImage","text":"An AppImage is a runtime ELF followed by a squashfs image. The runtime locates the filesystem directly after itself, mounts it through FUSE, and executes AppRun from the mount point. There is no container, no manifest, and no package database.\n\nAppDir → mksquashfs → runtime → MyApp.AppImage\n\nAppBundler has mksquashfs write the filesystem straight into the output file after a reserved prefix, then writes the runtime into that prefix. \n\nRuntimes are currently downloaded from AppImage/type2-runtime.\n\nFuture releases will retrieve runtimes through AppImageRuntime_jll to keep builds reproducible in the long term.","title":"Build pipeline"},{"category":"section","location":"formats/appimage/#Package-contents","page":"AppImage","text":"The staged AppDir holds the application payload and a single entry point, AppRun, which the runtime executes from the mount point. AppBundler generates AppRun from a template that can be overridden in meta/appimage/AppRun.sh.\n\nThe AppImage specification also defines desktop integration files such as myapp.desktop, myapp.png, and .DirIcon. In practice, AppImage desktop integration has never matured and these files have little effect, so AppBundler does not generate them.","title":"Package contents"},{"category":"section","location":"formats/appimage/#Installation","page":"AppImage","text":"An AppImage needs no installation: make it executable with chmod +x and run it. Mounting requires fusermount on the target machine. Where it is missing, the runtime suggests --appimage-extract-and-run, which extracts to a temporary directory first. ","title":"Installation"},{"category":"section","location":"formats/appimage/#User-data","page":"AppImage","text":"The mounted filesystem is read-only and disappears when the application exits. Without an installation step, an AppImage cannot tell whether it is run once or kept long-term. By default, user data, including JULIA_DEPOT_PATH, therefore goes to a temporary directory. To keep it, set USER_DATA:\n\nUSER_DATA=~/mydata ./myapp.AppImage\n\nTo make this permanent, move the AppImage to ~/Applications and add an alias to your shell profile:\n\nalias myapp='USER_DATA=~/mydata ~/Applications/myapp.AppImage'\n\nFor further customisation, edit AppRun during packaging, for example to set USER_DATA to $XDG_DATA_HOME/<app>.","title":"User data"},{"category":"section","location":"formats/appimage/#Extraction","page":"AppImage","text":"Since the payload is an ordinary squashfs at a known offset, it can be extracted directly, for example where FUSE is unavailable or to inspect the contents:\n\nunsquashfs -o $(./MyApp-1.0.0-x86_64.AppImage --appimage-offset) -d extracted MyApp-1.0.0-x86_64.AppImage\n\n--appimage-offset and AppBundler.AppImagePack.offset both return the size of the runtime prefix.\n\nFuture releases will also support extraction with appbundler unpack.","title":"Extraction"},{"category":"section","location":"formats/appimage/#API","page":"AppImage","text":"","title":"API"},{"category":"type","location":"formats/appimage/#AppBundler.AppImage","page":"AppImage","text":"AppImage([project]; arch, compress, compression, runtime, kwargs...)\n\nCreate an AppImage configuration object for Linux application packaging.\n\nAppImage is a single executable file for Linux that is mounted rather than installed. It suits applications made of thousands of small files, which are slow to unpack and hydrate on the network filesystems used by HPC clusters.\n\nMounting requires fusermount on the target machine. Where it is missing the runtime falls back to --appimage-extract-and-run, and AppBundler.AppImagePack.unpack reads the payload without running the runtime at all.\n\nThe staged AppDir contains only the AppRun entry point and the application payload; no desktop entry, icon or AppStream metadata is included, so the AppImage does not integrate with desktop menus.\n\nArguments\n\nproject: Path to a project directory containing Project.toml, optional LocalPreferences.toml, and optional meta/appimage/ overrides\n\nKeyword Arguments\n\nprefix = joinpath(dirname(@__DIR__), \"recipes\"): Base directory or array of directories to search for configuration files in sequential order\napprun: Path to the AppRun template; resolved from prefix using the bundler predicate\ncommand::Cmd: Command launching the application; defaults to appimage.command preference. Its executable and escaped arguments are exposed to the templates as COMMAND\ncompression: squashfs compressor, one of :zstd (default), :gzip or :xz; defaults to the appimage.compression preference\nruntime: Path to the AppImage runtime. When unset, AppImageRuntime_jll is used if installed;\nwindowed: If true, the application runs without a console window; defaults to windowed preference\ncompress: If true, pack the AppDir into an .AppImage; defaults to compress preference\narch = Sys.ARCH: Target CPU architecture\npredicate: Bundler predicate used for hook selection; defaults to bundler preference\nparameters: Dictionary of parameters for Mustache template rendering. When project is provided, pre-populated from Project.toml and preferences\n\nExamples\n\nappimage_config = AppImage(app_dir)\n\nbundle(appimage_config, appimage_archive) do app_stage\n    # install application into app_stage\nend\n\n\n\n\n\n","title":"AppBundler.AppImage"},{"category":"section","location":"deployment/#Deployment","page":"Deployment","text":"Deploying an application involves compiling it for each target platform and distributing codesigned binaries that users can install. AppBundler supports cross-platform bundling — meaning a UNIX host can produce installers for multiple platforms — but Julia itself does not support cross-compilation. As a result, compilation must be performed natively on each target OS and architecture. This can be a significant burden for indie developers, which is where CI workflows become essential.\n\nThe one exception is macOS: because Apple Silicon Macs support Rosetta 2, an :aarch64 host can run :x86_64 binaries, so both architectures can be tested on a single machine. For all other platforms, you need a matching host, which is straightforward to obtain through GitHub Actions or similar CI infrastructure.","title":"Deployment"},{"category":"section","location":"deployment/#Code-Signing","page":"Deployment","text":"Both DMG (macOS) and MSIX (Windows) bundles must be codesigned before users can install them. Self-signed certificates are possible but require users to manually trust the certificate, which creates friction. To distribute without that friction, you need a certificate from a trusted provider.\n\nmacOS: Requires enrollment in the Apple Developer Program.\nWindows: Requires a commercial codesigning certificate. CERTUM currently offers the best value for open-source projects.\n\nAppBundler expects provider-issued certificates in .pfx format, placed at:\n\nmeta/dmg/certificate.pfx   # macOS\nmeta/msix/certificate.pfx  # Windows","title":"Code Signing"},{"category":"section","location":"deployment/#Testing-with-Self-Signed-Certificates","page":"Deployment","text":"Before purchasing a certificate, you can generate self-signed certificates to test the full signing workflow:\n\nAppBundler.generate_signing_certificates()\n\nThis prints two passwords to the terminal: MACOS_PFX_PASSWORD and WINDOWS_PFX_PASSWORD. You can then pass the password when building:\n\nappbundler build . --build-dir=build --password=\"{{MACOS_PFX_PASSWORD}}\"\n\nAlternatively, omitting --password will prompt for it interactively.","title":"Testing with Self-Signed Certificates"},{"category":"section","location":"deployment/#GitHub-Actions-Workflow","page":"Deployment","text":"GitHub Actions is the recommended CI solution for cross-platform Julia application deployment. It provides hosted runners for Windows, macOS, and Linux across all common architectures, making it straightforward to build and sign for every platform from a single workflow.\n\nAn example workflow is available at Release.yml. It can be installed into your project automatically by running:\n\nAppBundler.install_github_workflow()\n\nThis places the workflow file at .github/workflows/Release.yml, where GitHub picks it up. The workflow can be triggered manually from the Actions tab and will compile the application for all platforms, then attach the resulting installers to a new GitHub Release.\n\nOne configuration step is required: you must increase the default permissions granted to GitHub Actions so that the workflow can create releases and upload artifacts. This setting is found under Settings → Actions → General → Workflow permissions in your repository.\n\n(Image: GitHub Actions permissions)","title":"GitHub Actions Workflow"},{"category":"section","location":"deployment/#GitLab-CI-Workflow","page":"Deployment","text":"GitLab's shared runners are Linux-only, which limits its usefulness for Julia application deployment — macOS and Windows runners are available only as a paid add-on from some GitLab instances. If your project is already hosted on GitLab, a CI configuration that provides a similar deployment experience to the GitHub workflow above is available in the crypto-julia example repository.","title":"GitLab CI Workflow"},{"category":"section","location":"formats/snap/#Snap","page":"Snap","text":"Snap is a sandboxed application package format for Linux, supported across most major distributions. AppBundler builds .snap packages with a fully open-source toolchain, so they can be created on any platform without a Linux build environment.\n\n(Image: Snap installation)","title":"Snap"},{"category":"section","location":"formats/snap/#Build-pipeline","page":"Snap","text":"The standard workflow describes the build in snapcraft.yaml and lets snapcraft fetch sources, compile, and package the application:\n\nsnapcraft.yaml → snapcraft → MyApp.snap\n\nsnapcraft is open source, but it expects to run the compilation itself and does not cross-compile. Since a snap is ultimately a SquashFS image with metadata, AppBundler assembles the staging directory itself and compresses it directly:\n\nStagingDir → mksquashfs → MyApp.snap\n\nmksquashfs comes from squashfs-tools. It is redistributable and cross-compiled with BinaryBuilder, so snaps can be built from macOS and Windows as well as Linux.","title":"Build pipeline"},{"category":"section","location":"formats/snap/#Package-contents","page":"Snap","text":"Before compressing, AppBundler assembles StagingDir/ with the following layout:\n\nPath Purpose\nmeta/snap.yaml Package metadata and configuration\nbin/MyApp Application launcher\nmeta/icon.png Application icon\nmeta/gui/MyApp.desktop Desktop integration\nmeta/hooks/configure Configuration hook\n\nSandboxing and permissions are configured through the confinement level and the interfaces (plugs) declared in meta/snap.yaml.","title":"Package contents"},{"category":"section","location":"formats/snap/#Signing-and-installation","page":"Snap","text":"A locally built snap therefore has no assertions and must be installed with --dangerous. AppBundler applications usually use classic confinement, which also requires --classic:\n\nsudo snap install --classic --dangerous myapp.snap\n\nIf the application is configured for strict confinement in meta/snap.yaml, drop --classic. Snaps installed this way get a local revision such as x1 and receive no automatic updates.","title":"Signing and installation"},{"category":"section","location":"formats/snap/#SnapStore","page":"Snap","text":"Once a snap is published, the store-signed version can be installed offline, for example on air-gapped machines. Download the snap together with its assertions, then import the assertions before installing:\n\nsnap download myapp             # fetches myapp_N.snap and myapp_N.assert\nsudo snap ack myapp_N.assert\nsudo snap install myapp_N.snap\n\nSnaps preseeded into Ubuntu Core or custom images with ubuntu-image also carry their assertions and install without extra flags.","title":"SnapStore"},{"category":"section","location":"formats/snap/#Debugging","page":"Snap","text":"To debug Snap packages, skip compression and install the staging directory directly. You can also inspect the sandbox environment of the installed application:\n\nsudo snap try myapp_dir     # install unpackaged directory\nsnap run --shell myapp      # inspect sandbox environment interactively","title":"Debugging"},{"category":"section","location":"formats/snap/#API","page":"Snap","text":"snap_config = Snap(project)\n\nbundle(snap_config, snap_archive) do app_stage\n    # install files into app_stage\nend","title":"API"},{"category":"type","location":"formats/snap/#AppBundler.Snap","page":"Snap","text":"Snap([project]; arch, compress, windowed, kwargs...)\n\nCreate a Snap configuration object for Linux application packaging.\n\nWhen project is provided, configuration files are searched in project, then project/meta, then the built-in recipes directory. Application parameters (APP_NAME, APP_VERSION, etc.) are read from project/Project.toml, and packaging defaults (windowed, compress, etc.) are read from project/LocalPreferences.toml. Without project, only the built-in recipes and the active project's LocalPreferences.toml are used.\n\nArguments\n\nproject: Path to a project directory containing Project.toml, optional LocalPreferences.toml, and optional meta/snap/ overrides\n\nKeyword Arguments\n\nprefix = joinpath(dirname(@__DIR__), \"recipes\"): Base directory or array of directories to search for configuration files in sequential order\nicon = get_path(prefix, [\"snap/icon.png\", \"icon.png\"]): Path to application icon file\nsnap_config = get_path(prefix, \"snap/snap.yaml\"): Path to Snap package metadata template\ncommand::Cmd: Command launching the application; defaults to snap.command preference. Its executable and escaped arguments are exposed to the templates as COMMAND\ndesktop_launcher = get_path(prefix, \"snap/main.desktop\"): Path to desktop entry file template for GUI integration\nconfigure_hook: Path to configuration hook script run on snap set; resolved from prefix using the bundler predicate; omitted if not found\nmain_launcher: Path to main launcher script installed into bin/; resolved from prefix using the bundler predicate; omitted if not found\nwindowed: If true, the application runs without a console window; defaults to windowed preference\ncompress: If true, pack the staging directory into a .snap archive; defaults to compress preference\narch = Sys.ARCH: Target CPU architecture\npredicate: Bundler predicate used for hook selection; defaults to bundler preference\nparameters: Dictionary of parameters for Mustache template rendering. When project is provided, pre-populated from Project.toml and preferences: APP_NAME, APP_DISPLAY_NAME, APP_VERSION, BUILD_NUMBER, APP_SUMMARY, APP_DESCRIPTION, BUNDLE_IDENTIFIER, PUBLISHER_DISPLAY_NAME, MODULE_NAME (Julia-based bundles only), and WINDOWED\n\nExamples\n\nSnap()                                    # default recipes only\nSnap(app_dir)                             # project with Project.toml parameters\nSnap(app_dir; windowed = false)           # project with keyword overrides\nSnap(; prefix = [\"custom/\", \"recipes/\"]) # explicit search path\n\n\n\n\n\n","title":"AppBundler.Snap"},{"category":"section","location":"#AppBundler.jl","page":"Overview","text":"(Image: Docs) (Image: codecov)\n\n(Image: )\n\nAppBundler.jl is a Julia package for building native installers — Snap, MSIX, and DMG — using open-source tools available across all UNIX platforms (Linux, macOS, FreeBSD), while also being able to build MSIX on Windows itself. It replaces host system packaging utilities with cross-compiled tools distributed through Julia's Yggdrasil registry, resulting in a consistent and reproducible pipeline with no per-platform build environments required.\n\nThe package provides a clean API for bundling any Julia application exposing @main, with support for icons, configuration files, and native customization. GitHub Actions workflows allow multi-platform installer builds to be triggered with a single click, making continuous delivery of native installers practical for open-source Julia projects. AppBundler can also bundle applications written in other build systems (see docs).\n\n📖 Read the full documentation","title":"AppBundler.jl"},{"category":"section","location":"#Showcase","page":"Overview","text":"The following videos demonstrate the DMG and MSIX installers produced via AppBundler.\n\n(Image: DMG demo) (Image: MSIX demo)\nDMG MSIX\n\nNote: The bundling API has evolved and since MSIX video were recorded, but the pipeline structure and the resulting installer looks just the same.","title":"Showcase"},{"category":"section","location":"#Quickstart","page":"Overview","text":"If your Julia application exposes @main and can be launched with julia --project=. -m MyApp, it's ready to bundle. Here's how to get from source to a native installer in a few steps.","title":"Quickstart"},{"category":"section","location":"#1.-Install-AppBundler","page":"Overview","text":"]app add AppBundler\n\nMake sure ~/.julia/bin is on your PATH after installation.","title":"1. Install AppBundler"},{"category":"section","location":"#2.-Build-your-installer","page":"Overview","text":"From your project directory, run:\n\nappbundler build . --build-dir=build --selfsign\n\nThis detects your current platform and builds the appropriate installer — no extra configuration needed to get started.","title":"2. Build your installer"},{"category":"section","location":"#3.-Find-your-installer","page":"Overview","text":"Your installer will be in the build/ directory, ready to distribute. AppBundler detects your current platform and produces the corresponding installer:\n\nbuild/MyApp.msix on Windows\nbuild/MyApp.snap on Linux\nbuild/MyApp.dmg on macOS\n\nTo build for all three platforms at once, check out the included GitHub Actions workflow templates — they run each platform's build in parallel with a single push.","title":"3. Find your installer"},{"category":"section","location":"#Compilation-and-Customization","page":"Overview","text":"AppBundler offers a lot of room to grow as your project matures:\n\nPackage images — faster startup via precompiled pkgimages\nSysimage — even faster startup with a bundled system image\nJuliaC — native compilation (including the new --trim feature) for smaller installers\nSandboxing, terminal visibility, icons, and more — full control over how your app looks and behaves on each platform\n\nAppBundler can also bundle applications written in other build systems, and can build full Julia distributions — as demonstrated by Jumbo Julia.\n\n➡️ See the full documentation for all options and platform-specific configuration.","title":"Compilation & Customization"},{"category":"section","location":"#Supported-GUI-Frameworks","page":"Overview","text":"These frameworks have been tested with AppBundler. Minimal example applications for each are available in the examples folder — copy them straight from pkgdir(AppBundler) once installed.\n\nFramework Platform Support Notes Examples\nQML ✓ All platforms Fully supported PeaceFounderClient\nGLFW ✓ All platforms Fully supported —\nElectron ✓ All platforms Fully supported BonitoBook\nGtk/Mousetrap ⚠ macOS, Linux Does not launch on Windows —\nMakie ⚠ All platforms GLMakie may not work on Windows ImageColorThresholderApp\nBlink ⚠ All platforms Requires heavy patching for relocatability KomaMRI","title":"Supported GUI Frameworks"},{"category":"section","location":"#Installing-Built-Applications","page":"Overview","text":"Sharing your app with end users? Here's what they need to know.\n\nMSIX (Windows): For self-signed builds, open the MSIX properties and add the certificate to Trusted Root Certification Authorities first (step-by-step guide). Then double-click the installer.\nSnap (Linux): Install from the terminal: snap install --classic --dangerous MyApp.snap\nDMG (macOS): For self-signed builds, click the app, then go to Settings → Privacy & Security to approve the launch request. Drag it to Applications, launch it, and approve once more in Privacy & Security if prompted.\n\nThese extra steps are only needed for self-signed builds. Purchasing Windows and macOS code signing certificates eliminates them entirely. For Snap, submitting to the Snap Store enables one-click GUI installation.","title":"Installing Built Applications"},{"category":"section","location":"#Acknowledgments","page":"Overview","text":"This work is supported by the European Union through the Next Generation Internet initiative (NGI0 Entrust), via the NLnet Julia-AppBundler project.","title":"Acknowledgments"}]
}
