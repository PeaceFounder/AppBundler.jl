name: {{APP_EXE}}
title: {{APP_DISPLAY_NAME}}
base: core22
version: '{{APP_VERSION}}'
summary: {{APP_SUMMARY}} 
description: {{APP_DESCRIPTION}}
grade: devel
confinement: classic 

apps:
  {{APP_EXE}}:
    command: {{{COMMAND}}}
    desktop: meta/gui/{{APP_EXE}}.desktop
    plugs:
      - opengl
      - desktop
      - desktop-legacy
      - wayland
      - x11
      - network

{{#JULIAIMG}}

hooks:
  configure:
    plugs:
      - network
      - opengl
      - wayland

{{/JULIAIMG}}