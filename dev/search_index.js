var documenterSearchIndex = {"docs":
[{"location":"customization/#Customization","page":"Customization","title":"Customization","text":"Every operating system has made unique design choices for application distribution. On macOS, applications are placed in the Applications folder via DMG containers. Windows supports many installer formats, with MSIX being the most modern. Linux uses Snap and Flatpak for distributing external software. Creating an installer on each platform involves a common set of tasks:\n\nProducing icon assets in the format the installer or operating system expects\nDeclaring the capabilities required by the application\nSpecifying the launch entry point, whether GUI or terminal\nBundling all configuration files with the application\nCode-signing the installer and, where required, the application itself\n\nMaintaining these platform-specific details is burdensome. AppBundler addresses this with sensible defaults that make shipping GUI applications straightforward. Where customization is needed, developers can apply a configuration overlay, keeping the process easy to debug and reason about.","category":"section"},{"location":"customization/#How-It-Works","page":"Customization","title":"How It Works","text":"A build follows this pipeline:\n\nProject.toml          ← app name, version\nLocalPreferences.toml ← all build parameters\nmeta/                 ← optional file overrides\n        ↓\nappbundler build . --build-dir=build\n        ↓\nbuild/<name>.{msix,snap,dmg}\n\nMost builds require only a short LocalPreferences.toml. Build customization is done by placing files in meta/ that override AppBundler's built-in bundle templates — no changes to the core tool needed.\n\nIterating quickly: Use --debug for faster iteration when troubleshooting packaging or sandboxing issues — see Surgical Overrides.","category":"section"},{"location":"customization/#Command-Line-Parameters","page":"Customization","title":"Command-Line Parameters","text":"using AppBundler # hide\nAppBundler.print_help() # hide","category":"section"},{"location":"customization/#Preferences","page":"Customization","title":"Preferences","text":"Parameters are read from LocalPreferences.toml and from Project.toml (for the module name and application version; LocalPreferences.toml can override these). The full list of available parameters is in joinpath(pkgdir(AppBundler), \"LocalPreferences.toml\").\n\nTo enable AppBundler preferences, add the following to your application's Project.toml; otherwise the preferences for AppBundler will not be registered:\n\n[extras]\nAppBundler = \"40eb83ae-c93a-480c-8f39-f018b568f472\"\n\nA typical LocalPreferences.toml is short:\n\n[AppBundler]\nwindowed = false\nbundler = \"juliac\"\njuliac_trim = true","category":"section"},{"location":"customization/#Quick-Reference","page":"Customization","title":"Quick Reference","text":"Parameter Default Description\nMetadata  \napp_name from Project.toml Application name\nversion from Project.toml Application version\napp_summary — Short description for MSIX and Snap\napp_description — Longer description for Snap\npublisher_name — Publisher name\nbundle_identifier org.appbundler.{{app_name}} Bundle identifier for DMG\nbuild_number git commit count Falls back to 0 if git is unavailable\nCommon  \nwindowed false Hide a console window at runtime\ncompress true Compress the application inside the bundle\nselfsign false Sign with a self-signed certificate\noverwrite_target false Overwrite target path (--force)\nBundler  \nbundler juliaimg Bundler to use: juliaimg or juliac\njuliaimg_mainless false Launch bin/julia directly without calling main\njuliaimg_precompile true Precompile project modules\njuliaimg_incremental false Build cache on top of Julia's own rather than starting fresh\njuliaimg_sysimg [] Packages to bake into the system image\njuliaimg_selective_assets false Enable selective asset inclusion (see AppEnv)\njuliac_trim false Enable trimming when compiling with juliac\nMSIX  \nmsix_path_length_threshold 260 Maximum allowed path length within the bundle\nmsix_skip_long_paths false Skip paths exceeding the threshold instead of erroring\nmsix_skip_symlinks true Skip symlinks\nmsix_skip_unicode_paths true Skip Unicode paths instead of erroring\nmsix_publisher \"CN=AppBundler, C=XX, O=PeaceFounder\" Publisher string for AppxManifest.xml\nDMG  \ndmg_shallow_signing true Sign only the top-level binary\ndmg_hardened_runtime true Enable hardened runtime during signing\ndmg_sandboxed_runtime false Restrict access to peripherals and system directories\ndmg_compression lzma Compression algorithm: bzip2, zlib, lzma, or lzfse\n\nBundler. The bundler choice determines which recipe files are applied and which juliaimg_* or juliac_* parameters are relevant. juliaimg_mainless is intended for Julia distributions that launch bin/julia directly rather than calling an application main. juliaimg_sysimg only needs top-level packages — dependencies are baked in automatically. juliaimg_selective_assets requires modules to be in the sysimage, since it removes all source files from the bundle; with the juliac bundler, selective assets are always used.\n\nMSIX. Windows does not support long paths inside bundles, so msix_path_length_threshold and msix_skip_long_paths exist to either warn or skip offending paths rather than erroring out. The msix_publisher string must match the signing certificate exactly; AppBundler reads it from the certificate at bundle time and inlines it into AppxManifest.xml automatically, so manual edits are rarely needed.\n\nDMG. dmg_shallow_signing must be set to false when submitting for Apple notarization, as all binaries must be signed individually. dmg_sandboxed_runtime restricts access to peripherals and system directories and should only be enabled if your application is designed to run in a sandbox.","category":"section"},{"location":"customization/#AppEnv","page":"Customization","title":"AppEnv","text":"AppEnv is a small runtime support library that bridges the gap between a bundled Julia application and the host operating system. It handles three concerns that every bundled Julia app needs: environment setup, user data directories, and asset location.\n\nAppEnv is the first module loaded in juliaimg bundles. At runtime it configures LOAD_PATH and DEPOT_PATH so the application uses its compiled precompilation cache correctly. For Snap applications, the precompilation cache can be generated during installation via a configure hook, which AppEnv also provides.","category":"section"},{"location":"customization/#User-Data-Directory","page":"Customization","title":"User Data Directory","text":"At launch, AppEnv sets the AppEnv.USER_DATA variable to a platform-appropriate writable location where the application can store settings, caches, and other persistent data. On Snap and MSIX the directory is managed by the operating system and is removed automatically when the application is uninstalled. The location can always be overridden by setting the USER_DATA environment variable before launch.\n\nDMG — ~/.config/<app_name> (the depot goes to ~/.cache/<app_name>)\nDMG (sandboxed) — ~/Library/Application Support/Local (detected via APP_SANDBOX_CONTAINER_ID)\nMSIX — %LOCALAPPDATA%\\Packages\\<bundle_identifier>_<hash>\\LocalState\nSnap — $SNAP_USER_DATA (set directly from the Snap environment variable)","category":"section"},{"location":"customization/#Asset-Management","page":"Customization","title":"Asset Management","text":"AppEnv initializes pkgorigins from an index created at compile time. This allows assets to be placed within package directories and referenced via pkgdir(@__MODULE__) in a relocatable way, while only including a selected subset of files.\n\nAssets are declared per-module in LocalPreferences.toml using the assets key:\n\n[AppEnv]\nassets = [\"LICENSE\"]\n\n[QMLApp]\nassets = [\"src/App.qml\"]\n\n[AppBundler]\n# AppBundler options\n\nAssets are stored under assets/AppEnv and assets/QMLApp in the main directory. Package developers can declare their runtime assets here, while application developers can override them non-invasively.\n\nSelective assets are optional with juliaimg (enabled via juliaimg_selective_assets, which removes all source code from the bundle), but are the only mode available with the juliac bundler.","category":"section"},{"location":"customization/#Application-Structure","page":"Customization","title":"Application Structure","text":"When using JuliaC, the recommended entry point is:\n\nusing AppEnv\n\nfunction (@main)(ARGS)\n    AppEnv.init()\n    # Application logic; optionally reference AppEnv.USER_DATA\nend\n\nAppEnv.init() loads pkgorigins from a stored index within the compiled application so the runtime can locate assets and sets up USER_DATA. In juliaimg bundles it is called implicitly via etc/julia/startup.jl, so USER_DATA is available without any explicit call. With juliac, it must be called explicitly as shown above. In an interactive Julia session it does nothing, so it can be left in place during development without affecting application behaviour. It compiles correctly with JuliaC when trimming is enabled and is covered by the test suite.","category":"section"},{"location":"customization/#Surgical-Overrides","page":"Customization","title":"Surgical Overrides","text":"AppBundler is designed for surgical customization through native file overrides. Files placed in the application's meta/ directory override AppBundler's default templates located at $(pkgdir(AppBundler))/recipes/. Templates are kept intentionally simple — rather than providing complex nested templates, AppBundler encourages copying and modifying complete configuration files, which keeps platform-specific customization straightforward to debug and communicate about.\n\nTip: Use --debug when working through override changes. It produces an uncompressed, self-signed bundle quickly and opens a console window so you can observe runtime behaviour without waiting for a full release build.","category":"section"},{"location":"customization/#Override-Locations","page":"Customization","title":"Override Locations","text":"Format Directory Files\nDMG meta/dmg/ Entitlements.plist, Info.plist, DS_Store.toml, juliac_main.sh, juliaimg_main.sh\nMSIX meta/msix/ AppxManifest.xml, MSIXAppInstallerData.xml, resources.pri\nSnap meta/snap/ snap.yaml, main.desktop, juliaimg_main.sh, juliaimg_configure.sh\nAll meta/ icon.png, icon.icns, startup.jl (juliaimg only)","category":"section"},{"location":"customization/#Icons","page":"Customization","title":"Icons","text":"To use a custom application icon, place icon.png and icon.icns in the meta/ directory. During bundling, AppBundler checks for these files first and falls back to its built-in defaults if they are not present.","category":"section"},{"location":"customization/#Template-Variables","page":"Customization","title":"Template Variables","text":"Configuration files are Mustache templates. Variables are inlined at bundle time as capitalized versions of the preference names. For example, meta/snap/main.desktop:\n\n[Desktop Entry]\nName={{APP_DISPLAY_NAME}}\nExec={{APP_NAME}}\nIcon=${SNAP}/meta/icon.png\nVersion={{APP_VERSION}}\nComment={{APP_SUMMARY}}\nTerminal={{#WINDOWED}}false{{/WINDOWED}}{{^WINDOWED}}true{{/WINDOWED}}\nType=Application\nCategories=Utility;\n\nThe {{#WINDOWED}}...{{/WINDOWED}} / {{^WINDOWED}}...{{/WINDOWED}} pattern implements conditional logic driven by the windowed preference. When overriding a template, variables may be kept or replaced with static values.\n\nSome files are not installed directly into the bundle but are used as inputs during the build. meta/dmg/Entitlements.plist configures the sandbox baked into the signature of the main launcher, and meta/dmg/DS_Store.toml is a user-editable TOML representation of .DS_Store that is compiled into a binary .DS_Store before being placed in the DMG.","category":"section"},{"location":"customization/#Bundler-Specific-Files","page":"Customization","title":"Bundler-Specific Files","text":"Some files only apply to a specific bundler, indicated by a juliac_ or juliaimg_ prefix. For instance, meta/snap/juliaimg_main.sh is picked up only when using juliaimg:\n\n#!/bin/bash\n\nSCRIPT_DIR=$(dirname \"$0\")\n\nJULIA=\"$SCRIPT_DIR/julia\"\n$JULIA {{#MODULE_NAME}}--eval=\"using {{MODULE_NAME}}\" -- {{/MODULE_NAME}} $@\n\nThe equivalent for juliac is meta/snap/juliac_main.sh (and meta/dmg/juliac_main.sh on macOS), which can point directly to the compiled binary. To override a launcher, place a replacement at the prefixed path (e.g. meta/snap/juliaimg_main.sh) to target only that bundler, or at the unprefixed path (e.g. meta/snap/main.sh) to apply to both. meta/startup.jl is specific to juliaimg bundles — it is placed in etc/julia/ inside the bundle and is responsible for calling AppEnv.init() implicitly.","category":"section"},{"location":"customization/#Sandboxing-and-Capabilities","page":"Customization","title":"Sandboxing and Capabilities","text":"To customize sandboxing — such as granting access to hardware, networking, or custom launchers — override the relevant configuration file in your meta/ folder: Entitlements.plist for DMG, AppxManifest.xml for MSIX, and snap.yaml for Snap. Use --debug to iterate quickly when working through capability changes, and refer to the troubleshooting guide if the application does not behave as expected.","category":"section"},{"location":"deployment/#Deployment","page":"Deployment","title":"Deployment","text":"Deploying an application involves compiling it for each target platform and distributing codesigned binaries that users can install. AppBundler supports cross-platform bundling — meaning a UNIX host can produce installers for multiple platforms — but Julia itself does not support cross-compilation. As a result, compilation must be performed natively on each target OS and architecture. This can be a significant burden for indie developers, which is where CI workflows become essential.\n\nThe one exception is macOS: because Apple Silicon Macs support Rosetta 2, an :aarch64 host can run :x86_64 binaries, so both architectures can be tested on a single machine. For all other platforms, you need a matching host, which is straightforward to obtain through GitHub Actions or similar CI infrastructure.","category":"section"},{"location":"deployment/#Code-Signing","page":"Deployment","title":"Code Signing","text":"Both DMG (macOS) and MSIX (Windows) bundles must be codesigned before users can install them. Self-signed certificates are possible but require users to manually trust the certificate, which creates friction. To distribute without that friction, you need a certificate from a trusted provider.\n\nmacOS: Requires enrollment in the Apple Developer Program.\nWindows: Requires a commercial codesigning certificate. CERTUM currently offers the best value for open-source projects.\n\nAppBundler expects provider-issued certificates in .pfx format, placed at:\n\nmeta/dmg/certificate.pfx   # macOS\nmeta/msix/certificate.pfx  # Windows","category":"section"},{"location":"deployment/#Testing-with-Self-Signed-Certificates","page":"Deployment","title":"Testing with Self-Signed Certificates","text":"Before purchasing a certificate, you can generate self-signed certificates to test the full signing workflow:\n\nAppBundler.generate_signing_certificates()\n\nThis prints two passwords to the terminal: MACOS_PFX_PASSWORD and WINDOWS_PFX_PASSWORD. You can then pass the password when building:\n\nappbundler build . --build-dir=build --password=\"{{MACOS_PFX_PASSWORD}}\"\n\nAlternatively, omitting --password will prompt for it interactively.","category":"section"},{"location":"deployment/#macOS-Notarization","page":"Deployment","title":"macOS Notarization","text":"macOS codesigning has an additional requirement beyond certificate signing: Apple requires all distributed applications to be notarized. Notarization means submitting the bundle to Apple's servers, where it is checked for proper structure and the absence of malware. Two settings must be enabled in LocalPreferences.toml for a bundle to pass notarization:\n\ndmg_shallow_signing = false\ndmg_hardened_runtime = true\n\nNote on shallow vs. deep signing: Deep signing is disabled by default because it takes considerable time for Julia applications and currently tends to fail with rcodesign deep signing. Unfortunately, codesign --verify --deep --verbose=4 myapp.app passes even with shallow signing, so the only reliable way to verify that deep signing is correct is to submit the bundle to Apple's notary service and inspect the response. Budget some time for this when setting up notarization for the first time.","category":"section"},{"location":"deployment/#Custom-Signing-Solutions","page":"Deployment","title":"Custom Signing Solutions","text":"Some certificate providers deliver keys inside secure hardware tokens rather than as a .pfx file. Hardware token integration is planned for a future release. In the meantime, users needing custom signing workflows can use the lower-level signing API described in reference.md.","category":"section"},{"location":"deployment/#GitHub-Actions-Workflow","page":"Deployment","title":"GitHub Actions Workflow","text":"GitHub Actions is the recommended CI solution for cross-platform Julia application deployment. It provides hosted runners for Windows, macOS, and Linux across all common architectures, making it straightforward to build and sign for every platform from a single workflow.\n\nAn example workflow is available at Release.yml. It can be installed into your project automatically by running:\n\nAppBundler.install_github_workflow()\n\nThis places the workflow file at .github/workflows/Release.yml, where GitHub picks it up. The workflow can be triggered manually from the Actions tab and will compile the application for all platforms, then attach the resulting installers to a new GitHub Release.\n\nOne configuration step is required: you must increase the default permissions granted to GitHub Actions so that the workflow can create releases and upload artifacts. This setting is found under Settings → Actions → General → Workflow permissions in your repository.\n\n(Image: GitHub Actions permissions)","category":"section"},{"location":"deployment/#GitLab-CI-Workflow","page":"Deployment","title":"GitLab CI Workflow","text":"GitLab's shared runners are Linux-only, which limits its usefulness for Julia application deployment — macOS and Windows runners are available only as a paid add-on from some GitLab instances. If your project is already hosted on GitLab, a CI configuration that provides a similar deployment experience to the GitHub workflow above is available in the crypto-julia example repository.","category":"section"},{"location":"troubleshooting/#Troubleshooting","page":"Troubleshooting","title":"Troubleshooting","text":"Sandboxed execution is a requirement across macOS, Windows, and Linux for distribution through official app marketplaces, where confinement is typically a prerequisite for approval. AppBundler supports sandboxing for all three bundle formats — MSIX, Snap, and DMG. Sandbox support for Julia applications is not yet fully resolved: MSIX and Snap both have known open issues, while DMG sandboxing has a working solution.\n\nEach format implements isolation through its own mechanism, requiring format-specific configuration. Currently, macOS sandboxing is functional via the workaround described below; Snap confinement is limited to GTK applications; and MSIX sandboxing is non-functional for Julia applications due to a dependency on the legacy msvcrt32.dll runtime, which is incompatible with the UWP execution model.\n\nTo facilitate debugging, bundle compression can be disabled via the --debug flag. This also enables a console window through which stdout/stderr output is surfaced.","category":"section"},{"location":"troubleshooting/#Snap-(Linux)","page":"Troubleshooting","title":"Snap (Linux)","text":"Snap confinement is currently broken for OpenGL applications; the root cause is unknown (see JuliaGraphics/QML.jl#191).\n\nTo debug Snap packages, skip compression and install the staging directory directly. Optionally, inspect the sandbox environment of the installed application:\n\nsnap try myapp_dir       # install unpackaged directory\nsnap run --shell myapp   # inspect sandbox environment interactively","category":"section"},{"location":"troubleshooting/#DMG-(macOS)","page":"Troubleshooting","title":"DMG (macOS)","text":"com.apple.coreservices.launchservicesd must be whitelisted as a sandbox exception; the underlying reason is not yet understood.\nNotarization is required for macOS distribution and can be performed with rcodesign.\nDeep signing (dmg_shallow_signing=false) may intermittently fail; if so, clean the staging directory and retry. For custom cleanup logic, use the public API as described in Reference.\nNotarization may fail due to an incompatible directory structure or unsigned artifacts; this has not yet been fully investigated.\n\nTo debug DMG bundles, skip the compression step. Note that the staging directory must carry a .app extension for macOS to treat it as an application bundle.","category":"section"},{"location":"troubleshooting/#MSIX-(Windows)","page":"Troubleshooting","title":"MSIX (Windows)","text":"GTK applications fail to resolve shared libraries within the UWP execution environment. This is expected to be resolved once Julia migrates to the ucrt.dll runtime.\nOnly the packagedClassicApp execution mode is supported due to Julia's msvcrt32.dll dependency. This constraint will be lifted once Julia transitions to ucrt.dll.\n\nTo debug MSIX packages, skip compression and register the manifest from the staging directory directly:\n\nAdd-AppPackage -register AppxManifest.xml","category":"section"},{"location":"reference/#Reference","page":"Reference","title":"Reference","text":"AppBundler is structured around two concepts: a product spec, which describes how to build a Julia application, and a bundle format, which describes how to package it for distribution on a target platform. These are combined through the bundle function:\n\nbundle(spec, format, destination; password = \"\")\n\nThis is also the function exposed to the command-line API, where command-line parameters configure the spec and format fields.","category":"section"},{"location":"reference/#Product-Specs","page":"Reference","title":"Product Specs","text":"A product spec defines how the application is compiled or staged. There are two kinds:\n\nspec = JuliaImgBundle(project; kwargs...)  # stages a self-contained Julia image\nspec = JuliaCBundle(project; kwargs...)    # compiles a native executable via juliac\n\nBoth specs can be staged directly into a directory for inspection before packaging via stage(spec, destination). For JuliaImgBundle, staging is platform-agnostic as long as compilation is not required — cross-platform staging is possible by setting precompile=false and sysimg_packages = [], with the target platform specified via the platform keyword (defaults to HostPlatform()). For JuliaCBundle, the platform is fixed to the host, as determined by the juliac executable on PATH.","category":"section"},{"location":"reference/#Bundle-Formats","page":"Reference","title":"Bundle Formats","text":"A bundle format defines the packaging target. The three supported formats are DMG (macOS), MSIX (Windows), and Snap (Linux). They are instantiated from a project directory:\n\ndmg  = DMG(project; arch = Sys.ARCH, kwargs...)\nmsix = MSIX(project; arch = Sys.ARCH, kwargs...)\nsnap = Snap(project; arch = Sys.ARCH, kwargs...)\n\nEach format reads configuration file overrides from the corresponding project/meta/<format> directory and carries architecture information that determines the destination platform. Bundle formats can also be staged independently via stage(format, destination) to produce the directory structure before compression and signing.","category":"section"},{"location":"reference/#Low-Level-Bundle-API","page":"Reference","title":"Low-Level Bundle API","text":"For packaging non-Julia applications, or when you need full control over what goes into the bundle, the lower-level bundle do-block API can be used directly:\n\nbundle(format, destination; password = \"\") do staging_dir\n    # copy or compile application files into staging_dir\nend\n\nThe password argument is the certificate password used to decrypt the signing certificate and perform code signing during the pack step. Non-Julia applications can be bundled this way as well, as long as the user takes care of building and installing the application files in the setup callback.","category":"section"},{"location":"reference/#Types","page":"Reference","title":"Types","text":"","category":"section"},{"location":"reference/#Functions","page":"Reference","title":"Functions","text":"","category":"section"},{"location":"reference/#AppBundler.JuliaImg.JuliaImgBundle","page":"Reference","title":"AppBundler.JuliaImg.JuliaImgBundle","text":"JuliaImgBundle(project; sysimg_packages = [], incremental = isempty(sysimg_packages), kwargs...)\n\nBuild specification for staging a Julia application as a self-contained image.\n\nThe Julia version is read from Manifest.toml in project (falling back to the running Julia version). Incremental compilation defaults to true when no sysimage packages are requested; building a sysimage forces incremental = false because a new sysimage invalidates all existing pkgimage caches.\n\nArguments\n\nproject::String: Path to the application directory containing Project.toml and Manifest.toml\n\nKeyword Arguments\n\nprecompile::Bool = true: Precompile packages during staging. When false, precompilation is deferred to the first launch on the target system\nincremental::Bool = isempty(sysimg_packages): Whether to reuse existing pkgimage caches. Automatically set to false when sysimg_packages is non-empty\nsysimg_packages::Vector{String} = []: Packages to bake into the system image. Triggers a full sysimage rebuild, which invalidates all pkgimage caches\nsysimg_args::Cmd =``: Extra command-line arguments forwarded to the sysimage compiler\nremove_sources::Bool = false: Strip .jl source files from the staged package tree (only meaningful when all relevant packages are baked into the sysimage)\nasset_spec::Dict{Symbol,Vector{String}} = Dict(): Selective asset inclusion rules. When empty, all artifacts are included and indexed via pkgorigin\nasset_rpath::String = \"assets\": Destination subdirectory for assets when asset_spec is set\nstartup_file::String: Path to the startup.jl template. Resolved from meta/startup.jl inside project, falling back to $(pkgdir(AppBundler))/recipes/startup.jl\n\nExamples\n\n# Default: precompile everything, incremental caches\npkg = JuliaImgBundle(\"path/to/app\")\n \n# Skip precompilation — compile on first launch instead\npkg = JuliaImgBundle(\"path/to/app\"; precompile = false)\n \n# Bake heavy dependencies into a sysimage (forces non-incremental rebuild)\npkg = JuliaImgBundle(\"path/to/app\"; sysimg_packages = [\"Plots\", \"DifferentialEquations\"])\n\n\n\n\n\nJuliaImgBundle(project; sysimg_packages = [], incremental = isempty(sysimg_packages), kwargs...)\n\nBuild specification for staging a Julia application as a self-contained image.\n\nThe Julia version is read from Manifest.toml in project (falling back to the running Julia version). Incremental compilation defaults to true when no sysimage packages are requested; building a sysimage forces incremental = false because a new sysimage invalidates all existing pkgimage caches.\n\nArguments\n\nproject::String: Path to the application directory containing Project.toml and Manifest.toml\n\nKeyword Arguments\n\nprecompile::Bool = true: Precompile packages during staging. When false, precompilation is deferred to the first launch on the target system\nincremental::Bool = isempty(sysimg_packages): Whether to reuse existing pkgimage caches. Automatically set to false when sysimg_packages is non-empty\nsysimg_packages::Vector{String} = []: Packages to bake into the system image. Triggers a full sysimage rebuild, which invalidates all pkgimage caches\nsysimg_args::Cmd =``: Extra command-line arguments forwarded to the sysimage compiler\nremove_sources::Bool = false: Strip .jl source files from the staged package tree (only meaningful when all relevant packages are baked into the sysimage)\nasset_spec::Dict{Symbol,Vector{String}} = Dict(): Selective asset inclusion rules. When empty, all artifacts are included and indexed via pkgorigin\nasset_rpath::String = \"assets\": Destination subdirectory for assets when asset_spec is set\nstartup_file::String: Path to the startup.jl template. Resolved from meta/startup.jl inside project, falling back to $(pkgdir(AppBundler))/recipes/startup.jl\n\nExamples\n\n# Default: precompile everything, incremental caches\npkg = JuliaImgBundle(\"path/to/app\")\n \n# Skip precompilation — compile on first launch instead\npkg = JuliaImgBundle(\"path/to/app\"; precompile = false)\n \n# Bake heavy dependencies into a sysimage (forces non-incremental rebuild)\npkg = JuliaImgBundle(\"path/to/app\"; sysimg_packages = [\"Plots\", \"DifferentialEquations\"])\n\n\n\n\n\n","category":"type"},{"location":"reference/#AppBundler.JuliaC.JuliaCBundle","page":"Reference","title":"AppBundler.JuliaC.JuliaCBundle","text":"JuliaCBundle(project; kwargs...)\n\nBuild specification for compiling a Julia application into a native executable via juliac.\n\nUnlike JuliaImgBundle, which stages a full Julia runtime alongside precompiled package images, JuliaCBundle ahead-of-time compiles the application into a standalone native executable. The juliac tool must be installed and is looked up in bin/juliac under each entry of DEPOT_PATH, with ~/.julia/bin as a final fallback.\n\nArguments\n\nproject::String: Path to the application directory containing Project.toml\n\nKeyword Arguments\n\njuliac_cmd::Cmd = Cmd([juliac()]): Command used to invoke juliac. Defaults to the first juliac executable found on DEPOT_PATH\nexecutable_name::String: Name of the produced executable. Defaults to the lowercase module name derived from Project.toml\ntrim::Bool = false: When true, passes --trim=safe to juliac, removing unreachable code from the output binary\nargs::Cmd =`: Additional arguments forwarded verbatim tojuliac`\nasset_spec::Dict{Symbol,Vector{String}} = Dict(): Selective asset inclusion rules. When empty, no assets are copied into the bundle\nasset_rpath::String = \"assets\": Destination subdirectory for assets inside destination\n\nExamples\n\n# Minimal: compile with defaults\npkg = JuliaCBundle(\"path/to/app\")\n \n# Enable dead-code trimming and a custom executable name\npkg = JuliaCBundle(\"path/to/app\"; executable_name = \"myapp\", trim = true)\n\n\n\n\n\nJuliaCBundle(project; kwargs...)\n\nBuild specification for compiling a Julia application into a native executable via juliac.\n\nUnlike JuliaImgBundle, which stages a full Julia runtime alongside precompiled package images, JuliaCBundle ahead-of-time compiles the application into a standalone native executable. The juliac tool must be installed and is looked up in bin/juliac under each entry of DEPOT_PATH, with ~/.julia/bin as a final fallback.\n\nArguments\n\nproject::String: Path to the application directory containing Project.toml\n\nKeyword Arguments\n\njuliac_cmd::Cmd = Cmd([juliac()]): Command used to invoke juliac. Defaults to the first juliac executable found on DEPOT_PATH\nexecutable_name::String: Name of the produced executable. Defaults to the lowercase module name derived from Project.toml\ntrim::Bool = false: When true, passes --trim=safe to juliac, removing unreachable code from the output binary\nargs::Cmd =`: Additional arguments forwarded verbatim tojuliac`\nasset_spec::Dict{Symbol,Vector{String}} = Dict(): Selective asset inclusion rules. When empty, no assets are copied into the bundle\nasset_rpath::String = \"assets\": Destination subdirectory for assets inside destination\n\nExamples\n\n# Minimal: compile with defaults\npkg = JuliaCBundle(\"path/to/app\")\n \n# Enable dead-code trimming and a custom executable name\npkg = JuliaCBundle(\"path/to/app\"; executable_name = \"myapp\", trim = true)\n\n\n\n\n\n","category":"type"},{"location":"reference/#AppBundler.DMG","page":"Reference","title":"AppBundler.DMG","text":"DMG([overlay]; arch, compress, windowed, kwargs...)\n\nCreate a DMG configuration object for macOS application packaging.\n\nWhen overlay is provided, configuration files are searched in overlay, then overlay/meta, then the built-in recipes directory. Application parameters (APP_NAME, APP_VERSION, etc.) are read from overlay/Project.toml, and packaging defaults (selfsign, compression, etc.) are read from overlay/LocalPreferences.toml. Without overlay, only the built-in recipes and the active project's LocalPreferences.toml are used.\n\nArguments\n\noverlay: Path to a project directory containing Project.toml, optional LocalPreferences.toml, and optional meta/dmg/ overrides\n\nKeyword Arguments\n\nprefix = joinpath(dirname(@__DIR__), \"recipes\"): Base directory or array of directories to search for configuration files in sequential order\nicon = get_path(prefix, [\"dmg/icon.icns\", \"dmg/icon.png\", \"icon.icns\"]): Path to application icon (.icns or .png)\ninfo_config = get_path(prefix, \"dmg/Info.plist\"): Path to Info.plist template with app metadata\nentitlements = get_path(prefix, \"dmg/Entitlements.plist\"): Path to entitlements file for code signing\ndsstore = get_path(prefix, [\"dmg/DS_Store.toml\", \"dmg/DS_Store\"]): Path to DS_Store file or TOML template for Finder window appearance\nselfsign: If true, generate a temporary self-signed certificate instead of using pfx_cert; defaults to selfsign preference\npfx_cert = get_path(prefix, \"dmg/certificate.pfx\"): Path to code signing certificate\nshallow_signing: If true, sign only the top-level bundle rather than all nested binaries; defaults to dmg_shallow_signing preference\nhardened_runtime: If true, enable hardened runtime during signing (required for notarization); defaults to dmg_hardened_runtime preference\nsandboxed_runtime: If true, enable the App Sandbox entitlement; defaults to dmg_sandboxed_runtime preference\nmain_launcher: Path to the Julia entry-point script. When set, a native redirect launcher is installed at Contents/MacOS/<app_name> and the script itself at Contents/Libraries/main; resolved from prefix using the bundler predicate; omitted if not found\nhfsplus = false: If true, use HFS+ filesystem when building the disk image otherwise uses ISO\nwindowed: If true, the application runs without a console window; defaults to windowed preference\ncompress: If true, pack the staging directory into a .dmg disk image; defaults to compress preference\ncompression: Compression algorithm for the disk image (:lzma, :bzip2, :zlib, or :lzfse); defaults to dmg_compression preference\narch = Sys.ARCH: Target CPU architecture\npredicate: Bundler predicate used for hook selection; defaults to bundler preference\nparameters: Dictionary of parameters for Mustache template rendering. When overlay is provided, pre-populated from Project.toml and preferences: APP_NAME, APP_DISPLAY_NAME, APP_VERSION, BUILD_NUMBER, APP_SUMMARY, APP_DESCRIPTION, BUNDLE_IDENTIFIER, PUBLISHER_DISPLAY_NAME, MODULE_NAME (Julia-based bundles only), WINDOWED, and SANDBOXED_RUNTIME\n\nExamples\n\nDMG()                                    # default recipes only\nDMG(app_dir)                             # overlay with Project.toml parameters\nDMG(app_dir; hardened_runtime = false)   # overlay with keyword overrides\nDMG(; prefix = [\"custom/\", \"recipes/\"]) # explicit search path\n\n\n\n\n\n","category":"type"},{"location":"reference/#AppBundler.MSIX","page":"Reference","title":"AppBundler.MSIX","text":"MSIX([overlay]; arch, compress, windowed, kwargs...)\n\nCreate an MSIX configuration object for Windows application packaging.\n\nWhen overlay is provided, configuration files are searched in overlay, then overlay/meta, then the built-in recipes directory. Application parameters (APP_NAME, APP_VERSION, etc.) are read from overlay/Project.toml, and packaging defaults (path_length_threshold, selfsign, etc.) are read from overlay/LocalPreferences.toml. Without overlay, only the built-in recipes and the active project's LocalPreferences.toml are used.\n\nArguments\n\noverlay: Path to a project directory containing Project.toml, optional LocalPreferences.toml, and optional meta/msix/ overrides\n\nKeyword Arguments\n\nprefix = joinpath(dirname(@__DIR__), \"recipes\"): Base directory or array of directories to search for configuration files in sequential order\nicon = get_path(prefix, [\"msix/Assets\", \"msix/icon.png\", \"icon.png\"]; dir = true): Path to application icon file or Assets directory\nappxmanifest = get_path(prefix, \"msix/AppxManifest.xml\"): Path to MSIX application manifest template\nresources_pri = get_path(prefix, \"msix/resources.pri\"): Path to package resource index file\nmsixinstallerdata = get_path(prefix, \"msix/MSIXAppInstallerData.xml\"): Path to installer configuration template\npath_length_threshold: Maximum allowed path length; defaults to msix_path_length_threshold preference\nskip_long_paths: If true, skip files exceeding path length threshold; if false, throw an error; defaults to msix_skip_long_paths preference\nskip_symlinks: If true, skip file and directory symlinks; defaults to msix_skip_symlinks preference\nskip_unicode_paths: If true, skip files with non-ASCII paths; defaults to msix_skip_unicode_paths preference\nselfsign: If true, generate a temporary self-signed certificate instead of using pfx_cert; defaults to selfsign preference\npublisher: Publisher string embedded in the manifest; defaults to msix_publisher preference\npfx_cert = get_path(prefix, \"msix/certificate.pfx\"): Path to code signing certificate\nwindowed: If true, the application runs without a console window; defaults to windowed preference\ncompress: If true, pack the staging directory into an .msix archive; defaults to compress preference\narch = Sys.ARCH: Target CPU architecture\npredicate: Bundler predicate used for hook selection; defaults to bundler preference\nparameters: Dictionary of parameters for Mustache template rendering. When overlay is provided, pre-populated from Project.toml and preferences: APP_NAME, APP_DISPLAY_NAME, APP_VERSION, BUILD_NUMBER, APP_SUMMARY, APP_DESCRIPTION, BUNDLE_IDENTIFIER, PUBLISHER_DISPLAY_NAME, MODULE_NAME (Julia-based bundles only), WINDOWED, and PUBLISHER\n\nExamples\n\nMSIX()                                    # default recipes only\nMSIX(app_dir)                             # overlay with Project.toml parameters\nMSIX(app_dir; skip_long_paths = true)     # overlay with keyword overrides\nMSIX(; prefix = [\"custom/\", \"recipes/\"]) # explicit search path\n\n\n\n\n\n","category":"type"},{"location":"reference/#AppBundler.Snap","page":"Reference","title":"AppBundler.Snap","text":"Snap([overlay]; arch, compress, windowed, kwargs...)\n\nCreate a Snap configuration object for Linux application packaging.\n\nWhen overlay is provided, configuration files are searched in overlay, then overlay/meta, then the built-in recipes directory. Application parameters (APP_NAME, APP_VERSION, etc.) are read from overlay/Project.toml, and packaging defaults (windowed, compress, etc.) are read from overlay/LocalPreferences.toml. Without overlay, only the built-in recipes and the active project's LocalPreferences.toml are used.\n\nArguments\n\noverlay: Path to a project directory containing Project.toml, optional LocalPreferences.toml, and optional meta/snap/ overrides\n\nKeyword Arguments\n\nprefix = joinpath(dirname(@__DIR__), \"recipes\"): Base directory or array of directories to search for configuration files in sequential order\nicon = get_path(prefix, [\"snap/icon.png\", \"icon.png\"]): Path to application icon file\nsnap_config = get_path(prefix, \"snap/snap.yaml\"): Path to Snap package metadata template\ndesktop_launcher = get_path(prefix, \"snap/main.desktop\"): Path to desktop entry file template for GUI integration\nconfigure_hook: Path to configuration hook script run on snap set; resolved from prefix using the bundler predicate; omitted if not found\nmain_launcher: Path to main launcher script installed into bin/; resolved from prefix using the bundler predicate; omitted if not found\nwindowed: If true, the application runs without a console window; defaults to windowed preference\ncompress: If true, pack the staging directory into a .snap archive; defaults to compress preference\narch = Sys.ARCH: Target CPU architecture\npredicate: Bundler predicate used for hook selection; defaults to bundler preference\nparameters: Dictionary of parameters for Mustache template rendering. When overlay is provided, pre-populated from Project.toml and preferences: APP_NAME, APP_DISPLAY_NAME, APP_VERSION, BUILD_NUMBER, APP_SUMMARY, APP_DESCRIPTION, BUNDLE_IDENTIFIER, PUBLISHER_DISPLAY_NAME, MODULE_NAME (Julia-based bundles only), and WINDOWED\n\nExamples\n\nSnap()                                    # default recipes only\nSnap(app_dir)                             # overlay with Project.toml parameters\nSnap(app_dir; windowed = false)           # overlay with keyword overrides\nSnap(; prefix = [\"custom/\", \"recipes/\"]) # explicit search path\n\n\n\n\n\n","category":"type"},{"location":"reference/#AppBundler.bundle-Tuple{AppBundler.JuliaImg.JuliaImgBundle, AppBundler.DMG, String}","page":"Reference","title":"AppBundler.bundle","text":"bundle(product, packaging, destination; force=false, [password=\"\"])\n\nBundle a Julia application product into a platform-specific package at destination by calling bundle(setup, config, destination) with product staged as the setup step.\n\nArguments\n\nproduct: The application artifact to bundle. Either a JuliaImgBundle or a JuliaCBundle \npackaging: The target package format. One of:\nDMG — macOS disk image (.dmg). Must be built on macOS.\nSnap — Linux Snap package. Must be built on Linux.\nMSIX — Windows app package (.msix). Must be built on Windows.\ndestination: Output path for the produced package file.\n\nKeyword Arguments\n\nforce=false: Overwrite an existing package at destination if true.\npassword=\"\": Code-signing certificate password. Applicable to DMG and MSIX targets only.\n\nA warning is emitted when the host OS does not match the target platform, as cross-platform packaging is not supported.\n\nSee also stage, bundle(setup, config, destination).\n\n\n\n\n\n","category":"method"},{"location":"reference/#AppBundler.stage-Tuple{AppBundler.JuliaImg.JuliaImgBundle, String}","page":"Reference","title":"AppBundler.stage","text":"stage(product::JuliaImgBundle, destination::String;\n      platform::AbstractPlatform = HostPlatform(),\n      cpu_target = get_cpu_target(platform),\n      runtime_mode = \"MIN\",\n      app_name = \"\",\n      bundle_identifier = \"\")\n\nStage a Julia application into destination, ready for packaging and distribution.\n\nThe staging process:\n\nDownloads and extracts the Julia runtime for platform (version from Manifest.toml)\nCopies all non-stdlib packages and artifacts into the stdlib tree\nConfigures startup.jl, DEPOT_PATH, and LOAD_PATH via AppEnv\nOptionally compiles a system image from product.sysimg_packages\nOptionally precompiles pkgimages for all project dependencies\n\nWhen product.precompile is true, platform must match the host OS and architecture (see cross-compilation constraints below).\n\nArguments\n\nproduct::JuliaImgBundle: Staging configuration\ndestination::String: Directory in which the staged application is assembled\n\nKeyword Arguments\n\nplatform::AbstractPlatform: Target platform; defaults to the current host\ncpu_target: LLVM CPU target string; derived from platform by default\nruntime_mode: AppEnv runtime mode string passed to AppEnv.save_config\napp_name: Application name embedded in the AppEnv config\nbundle_identifier: Bundle identifier embedded in the AppEnv config (e.g. reverse-DNS on macOS)\n\nCross-Compilation Constraints\n\nPrecompilation runs native code, so cross-OS and cross-architecture compilation is not supported when product.precompile = true:\n\nWindows: host must be Windows\nmacOS: host must be macOS; x86_64 → aarch64 is not supported\nLinux: host must be Linux with a matching architecture\n\nSet precompile = false and sysimg_packages = [] in JuliaImgBundle to stage for a different platform without compilation.\n\nExamples\n\npkg = JuliaImgBundle(\"src/MyApp\")\n \n# Stage for the current machine\nstage(pkg, \"build/staging\")\n \n# Stage for macOS arm64 (must run on Apple Silicon with precompile=false, or natively)\nstage(pkg, \"build/MyApp.app/Contents/Resources/julia\";\n      platform = Platform(\"aarch64\", \"macos\"))\n \n# Embed app identity in the AppEnv config\nstage(pkg, \"build/staging\"; app_name = \"MyApp\", bundle_identifier = \"com.example.myapp\")\n\n\n\n\n\n","category":"method"},{"location":"reference/#AppBundler.stage-Tuple{AppBundler.JuliaC.JuliaCBundle, String}","page":"Reference","title":"AppBundler.stage","text":"stage(spec::JuliaCBundle, destination::String;\n      runtime_mode = \"MIN\",\n      app_name = get_module_name(spec.project),\n      bundle_identifier = \"\")\n\nCompile a Julia application into a native executable and assemble it in destination.\n\nThe staging process:\n\nSaves an AppEnv config to destination/config with runtime identity and load-path settings\nInstalls assets from spec.asset_spec into destination/<asset_rpath>\nWrites a pkgorigin index to destination/index for asset resolution at runtime\nInvokes juliac to AOT-compile the application and bundle the result into destination\n\nUnlike JuliaImgBundle, no Julia runtime tarball is downloaded — juliac produces a self-contained native binary. The host toolchain must be compatible with the target.\n\nArguments\n\nspec::JuliaCBundle: Compilation and asset configuration\ndestination::String: Directory in which the compiled application is assembled\n\nKeyword Arguments\n\nruntime_mode: AppEnv runtime mode string passed to AppEnv.save_config\napp_name: Application name embedded in the AppEnv config; defaults to the module name\nbundle_identifier: Bundle identifier embedded in the AppEnv config (e.g. reverse-DNS on macOS)\n\nExamples\n\npkg = JuliaCBundle(\"src/MyApp\")\n \n# Stage into a directory\nstage(pkg, \"build/myapp\";\n      app_name = \"MyApp\", bundle_identifier = \"com.example.myapp\")\n \n# Stage with a custom runtime mode\nstage(pkg, \"build/staging\"; runtime_mode = \"SANDBOX\")\n\n\n\n\n\n","category":"method"},{"location":"reference/#AppBundler.bundle-Tuple{Function, AppBundler.DMG, String}","page":"Reference","title":"AppBundler.bundle","text":"bundle(setup::Function, config, destination::String; force = false, kwargs...)\n\nStage, populate, and optionally compress an application bundle for distribution.\n\nconfig is a format-specific configuration object — MSIX (Windows), DMG (macOS), or Snap (Linux) — and destination is the path of the final artifact (e.g. \"MyApp.msix\", \"MyApp.dmg\", \"MyApp.snap\") or an uncompressed staging directory.\n\nThe function follows three steps:\n\nStage — writes platform metadata and directory structure into a staging area via stage.\nSetup — calls setup(staging_dir), where you copy or compile the application files that should be included in the bundle.\nPack — when config.compress is true (the default when destination carries the format extension), compresses the staging area into the final artifact and performs code signing.\n\nSet force = true to overwrite an existing destination path.\n\nCode signing\n\nMSIX and DMG sign the bundle automatically during the pack step. Pass the certificate password via the password keyword argument (defaults to \"\"). When config.selfsign is true, a temporary self-signed certificate is generated instead of using the one in the configuration. DMG entitlements are rendered from the template stored in the configuration. Snap packages are not signed locally; they are verified by the Snap Store after upload.\n\nExamples\n\nbundle(MSIX(app_dir), \"MyApp.msix\") do staging_dir\n    # copy or compile application files into staging_dir\nend\n \nbundle(DMG(app_dir), \"MyApp.dmg\") do staging_dir\n    # copy or compile application files into staging_dir\nend\n \nbundle(Snap(app_dir), \"MyApp.snap\") do staging_dir\n    # copy or compile application files into staging_dir\nend\n\n\n\n\n\n","category":"method"},{"location":"reference/#AppBundler.stage-Tuple{AppBundler.MSIX, String}","page":"Reference","title":"AppBundler.stage","text":"stage(config, destination::String; kwargs...)\n\nStage package metadata and directory structure into destination in preparation for bundling.\n\nconfig is a format-specific configuration object — MSIX, DMG, or Snap — that carries the template files, parameters, and settings for the target platform. Mustache-rendered templates are written using the parameters stored in config.\n\nstage is called automatically by bundle, but can be used directly when you need to inspect or modify the staging directory before compression and signing.\n\nStaged layout by format\n\nMSIX (stage(msix::MSIX, destination))\n\nAssets/ — application icons (generated from source or copied verbatim if already a directory)\nAppxManifest.xml — rendered package manifest\nresources.pri — package resource index\nMsix.AppInstaller.Data/MSIXAppInstallerData.xml — rendered installer configuration\n\nDMG (stage(dmg::DMG, destination; dsstore = false))\n\nContents/Resources/icon.icns — application icon\nContents/Info.plist — rendered application metadata\nContents/MacOS/<app-name> (optional) — native launcher when main_launcher is set\n\nWhen dsstore = true, also writes into the parent of destination:\n\nApplications — symlink to /Applications for drag-and-drop installation\n.DS_Store — custom Finder window appearance\n\nSnap (stage(snap::Snap, destination))\n\nmeta/icon.png — application icon\nmeta/snap.yaml — rendered Snap package metadata\nmeta/gui/<app-name>.desktop — rendered desktop launcher\nmeta/hooks/configure (optional) — configuration hook when configure_hook is set\nbin/<app-name> (optional) — main launcher script when main_launcher is set\n\nExamples\n\nstage(MSIX(app_dir), \"build/msix_staging\")\nstage(DMG(app_dir),  \"build/MyApp.app\"; dsstore = true)\nstage(Snap(app_dir), \"build/snap_staging\")\n\n\n\n\n\n","category":"method"},{"location":"#AppBundler.jl","page":"Overview","title":"AppBundler.jl","text":"(Image: Docs) (Image: codecov)\n\n(Image: )\n\nAppBundler.jl is a Julia package for building native installers — Snap, MSIX, and DMG — using open-source tools available across all UNIX platforms (Linux, macOS, FreeBSD), while also being able to build MSIX on Windows itself. It replaces host system packaging utilities with cross-compiled tools distributed through Julia's Yggdrasil registry, resulting in a consistent and reproducible pipeline with no per-platform build environments required.\n\nThe package provides a clean API for bundling any Julia application exposing @main, with support for icons, configuration files, and native customization. GitHub Actions workflows allow multi-platform installer builds to be triggered with a single click, making continuous delivery of native installers practical for open-source Julia projects. AppBundler can also bundle applications written in other build systems (see docs).\n\n📖 Read the full documentation","category":"section"},{"location":"#Showcase","page":"Overview","title":"Showcase","text":"The following videos demonstrate the DMG and MSIX installers produced via AppBundler.\n\n(Image: DMG demo) (Image: MSIX demo)\nDMG MSIX\n\nNote: The bundling API has evolved since these videos were recorded, but the pipeline structure and the resulting installers look just the same.","category":"section"},{"location":"#Quickstart","page":"Overview","title":"Quickstart","text":"If your Julia application exposes @main and can be launched with julia --project=. -m MyApp, it's ready to bundle. Here's how to get from source to a native installer in a few steps.","category":"section"},{"location":"#1.-Install-AppBundler","page":"Overview","title":"1. Install AppBundler","text":"]app add AppBundler\n\nMake sure ~/.julia/bin is on your PATH after installation.","category":"section"},{"location":"#2.-Build-your-installer","page":"Overview","title":"2. Build your installer","text":"From your project directory, run:\n\nappbundler build . --build-dir=build --selfsign\n\nThis detects your current platform and builds the appropriate installer — no extra configuration needed to get started.","category":"section"},{"location":"#3.-Find-your-installer","page":"Overview","title":"3. Find your installer","text":"Your installer will be in the build/ directory, ready to distribute. AppBundler detects your current platform and produces the corresponding installer:\n\nbuild/MyApp.msix on Windows\nbuild/MyApp.snap on Linux\nbuild/MyApp.dmg on macOS\n\nTo build for all three platforms at once, check out the included GitHub Actions workflow templates — they run each platform's build in parallel with a single push.","category":"section"},{"location":"#Compilation-and-Customization","page":"Overview","title":"Compilation & Customization","text":"AppBundler offers a lot of room to grow as your project matures:\n\nPackage images — faster startup via precompiled pkgimages\nSysimage — even faster startup with a bundled system image\nJuliaC — native compilation (including the new --trim feature) for smaller installers\nSandboxing, terminal visibility, icons, and more — full control over how your app looks and behaves on each platform\n\nAppBundler can also bundle applications written in other build systems, and can build full Julia distributions — as demonstrated by Jumbo Julia.\n\n➡️ See the full documentation for all options and platform-specific configuration.","category":"section"},{"location":"#Supported-GUI-Frameworks","page":"Overview","title":"Supported GUI Frameworks","text":"These frameworks have been tested with AppBundler. Minimal example applications for each are available in the examples folder — copy them straight from pkgdir(AppBundler) once installed.\n\nFramework Platform Support Notes Examples\nQML ✓ All platforms Fully supported PeaceFounderClient\nGLFW ✓ All platforms Fully supported —\nElectron ✓ All platforms Fully supported BonitoBook\nGtk/Mousetrap ⚠ macOS, Linux Does not launch on Windows —\nMakie ⚠ All platforms GLMakie may not work on Windows ImageColorThresholderApp\nBlink ⚠ All platforms Requires heavy patching for relocatability KomaMRI","category":"section"},{"location":"#Installing-Built-Applications","page":"Overview","title":"Installing Built Applications","text":"Sharing your app with end users? Here's what they need to know.\n\nMSIX (Windows): For self-signed builds, open the MSIX properties and add the certificate to Trusted Root Certification Authorities first (step-by-step guide). Then double-click the installer.\nSnap (Linux): Install from the terminal: snap install --classic --dangerous MyApp.snap\nDMG (macOS): For self-signed builds, click the app, then go to Settings → Privacy & Security to approve the launch request. Drag it to Applications, launch it, and approve once more in Privacy & Security if prompted.\n\nThese extra steps are only needed for self-signed builds. Purchasing Windows and macOS code signing certificates eliminates them entirely. For Snap, submitting to the Snap Store enables one-click GUI installation.","category":"section"},{"location":"#Acknowledgments","page":"Overview","title":"Acknowledgments","text":"This work is supported by the European Union through the Next Generation Internet initiative (NGI0 Entrust), via the NLnet Julia-AppBundler project.","category":"section"}]
}
